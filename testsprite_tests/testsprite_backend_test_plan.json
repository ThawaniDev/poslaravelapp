[
  {
    "id": "TC0001",
    "title": "GET api/v2/accessibility/preferences",
    "description": "Smoke test for GET /api/v2/accessibility/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accessibility/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0002",
    "title": "PUT api/v2/accessibility/preferences",
    "description": "Smoke test for PUT /api/v2/accessibility/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/accessibility/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0003",
    "title": "DELETE api/v2/accessibility/preferences",
    "description": "Smoke test for DELETE /api/v2/accessibility/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/accessibility/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0004",
    "title": "GET api/v2/accessibility/shortcuts",
    "description": "Smoke test for GET /api/v2/accessibility/shortcuts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accessibility/shortcuts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0005",
    "title": "PUT api/v2/accessibility/shortcuts",
    "description": "Smoke test for PUT /api/v2/accessibility/shortcuts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/accessibility/shortcuts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0006",
    "title": "GET api/v2/accounting/auto-export",
    "description": "Smoke test for GET /api/v2/accounting/auto-export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accounting/auto-export once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0007",
    "title": "PUT api/v2/accounting/auto-export",
    "description": "Smoke test for PUT /api/v2/accounting/auto-export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/accounting/auto-export once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0008",
    "title": "POST api/v2/accounting/connect",
    "description": "Smoke test for POST /api/v2/accounting/connect. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/accounting/connect once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0009",
    "title": "POST api/v2/accounting/disconnect",
    "description": "Smoke test for POST /api/v2/accounting/disconnect. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/accounting/disconnect once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0010",
    "title": "POST api/v2/accounting/exports",
    "description": "Smoke test for POST /api/v2/accounting/exports. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/accounting/exports once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0011",
    "title": "POST api/v2/accounting/exports/{id}/retry",
    "description": "Smoke test for POST /api/v2/accounting/exports/{id}/retry. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/accounting/exports/{id}/retry once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0012",
    "title": "GET api/v2/accounting/exports",
    "description": "Smoke test for GET /api/v2/accounting/exports. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accounting/exports once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0013",
    "title": "GET api/v2/accounting/exports/{id}",
    "description": "Smoke test for GET /api/v2/accounting/exports/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accounting/exports/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0014",
    "title": "GET api/v2/accounting/mapping",
    "description": "Smoke test for GET /api/v2/accounting/mapping. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accounting/mapping once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0015",
    "title": "PUT api/v2/accounting/mapping",
    "description": "Smoke test for PUT /api/v2/accounting/mapping. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/accounting/mapping once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0016",
    "title": "DELETE api/v2/accounting/mapping/{id}",
    "description": "Smoke test for DELETE /api/v2/accounting/mapping/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/accounting/mapping/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0017",
    "title": "GET api/v2/accounting/pos-account-keys",
    "description": "Smoke test for GET /api/v2/accounting/pos-account-keys. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accounting/pos-account-keys once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0018",
    "title": "POST api/v2/accounting/refresh-token",
    "description": "Smoke test for POST /api/v2/accounting/refresh-token. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/accounting/refresh-token once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0019",
    "title": "GET api/v2/accounting/status",
    "description": "Smoke test for GET /api/v2/accounting/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/accounting/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0020",
    "title": "POST api/v2/admin/ab-tests",
    "description": "Smoke test for POST /api/v2/admin/ab-tests. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/ab-tests with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0021",
    "title": "POST api/v2/admin/ab-tests/{testId}/start",
    "description": "Smoke test for POST /api/v2/admin/ab-tests/{testId}/start. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/ab-tests/{testId}/start with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0022",
    "title": "POST api/v2/admin/ab-tests/{testId}/stop",
    "description": "Smoke test for POST /api/v2/admin/ab-tests/{testId}/stop. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/ab-tests/{testId}/stop with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0023",
    "title": "POST api/v2/admin/ab-tests/{testId}/variants",
    "description": "Smoke test for POST /api/v2/admin/ab-tests/{testId}/variants. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/ab-tests/{testId}/variants with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0024",
    "title": "GET api/v2/admin/ab-tests",
    "description": "Smoke test for GET /api/v2/admin/ab-tests. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/ab-tests with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0025",
    "title": "GET api/v2/admin/ab-tests/{testId}",
    "description": "Smoke test for GET /api/v2/admin/ab-tests/{testId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/ab-tests/{testId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0026",
    "title": "GET api/v2/admin/ab-tests/{testId}/results",
    "description": "Smoke test for GET /api/v2/admin/ab-tests/{testId}/results. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/ab-tests/{testId}/results with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0027",
    "title": "PUT api/v2/admin/ab-tests/{testId}",
    "description": "Smoke test for PUT /api/v2/admin/ab-tests/{testId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/ab-tests/{testId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0028",
    "title": "DELETE api/v2/admin/ab-tests/{testId}",
    "description": "Smoke test for DELETE /api/v2/admin/ab-tests/{testId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/ab-tests/{testId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0029",
    "title": "DELETE api/v2/admin/ab-tests/{testId}/variants/{variantId}",
    "description": "Smoke test for DELETE /api/v2/admin/ab-tests/{testId}/variants/{variantId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/ab-tests/{testId}/variants/{variantId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0030",
    "title": "GET api/v2/admin/activity-log",
    "description": "Smoke test for GET /api/v2/admin/activity-log. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/activity-log with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0031",
    "title": "POST api/v2/admin/add-ons",
    "description": "Smoke test for POST /api/v2/admin/add-ons. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/add-ons with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0032",
    "title": "GET api/v2/admin/add-ons",
    "description": "Smoke test for GET /api/v2/admin/add-ons. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/add-ons with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0033",
    "title": "GET api/v2/admin/add-ons/{addOnId}",
    "description": "Smoke test for GET /api/v2/admin/add-ons/{addOnId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/add-ons/{addOnId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0034",
    "title": "PUT api/v2/admin/add-ons/{addOnId}",
    "description": "Smoke test for PUT /api/v2/admin/add-ons/{addOnId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/add-ons/{addOnId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0035",
    "title": "DELETE api/v2/admin/add-ons/{addOnId}",
    "description": "Smoke test for DELETE /api/v2/admin/add-ons/{addOnId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/add-ons/{addOnId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0036",
    "title": "GET api/v2/admin/analytics/daily-stats",
    "description": "Smoke test for GET /api/v2/admin/analytics/daily-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/daily-stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0037",
    "title": "GET api/v2/admin/analytics/dashboard",
    "description": "Smoke test for GET /api/v2/admin/analytics/dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/dashboard with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0038",
    "title": "POST api/v2/admin/analytics/export/revenue",
    "description": "Smoke test for POST /api/v2/admin/analytics/export/revenue. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/analytics/export/revenue with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0039",
    "title": "POST api/v2/admin/analytics/export/stores",
    "description": "Smoke test for POST /api/v2/admin/analytics/export/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/analytics/export/stores with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0040",
    "title": "POST api/v2/admin/analytics/export/subscriptions",
    "description": "Smoke test for POST /api/v2/admin/analytics/export/subscriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/analytics/export/subscriptions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0041",
    "title": "GET api/v2/admin/analytics/feature-stats",
    "description": "Smoke test for GET /api/v2/admin/analytics/feature-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/feature-stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0042",
    "title": "GET api/v2/admin/analytics/features",
    "description": "Smoke test for GET /api/v2/admin/analytics/features. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/features with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0043",
    "title": "GET api/v2/admin/analytics/health",
    "description": "Smoke test for GET /api/v2/admin/analytics/health. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/health with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0044",
    "title": "GET api/v2/admin/analytics/notifications",
    "description": "Smoke test for GET /api/v2/admin/analytics/notifications. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/notifications with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0045",
    "title": "GET api/v2/admin/analytics/plan-stats",
    "description": "Smoke test for GET /api/v2/admin/analytics/plan-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/plan-stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0046",
    "title": "GET api/v2/admin/analytics/revenue",
    "description": "Smoke test for GET /api/v2/admin/analytics/revenue. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/revenue with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0047",
    "title": "GET api/v2/admin/analytics/store-health",
    "description": "Smoke test for GET /api/v2/admin/analytics/store-health. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/store-health with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0048",
    "title": "GET api/v2/admin/analytics/stores",
    "description": "Smoke test for GET /api/v2/admin/analytics/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/stores with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0049",
    "title": "GET api/v2/admin/analytics/subscriptions",
    "description": "Smoke test for GET /api/v2/admin/analytics/subscriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/subscriptions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0050",
    "title": "GET api/v2/admin/analytics/support",
    "description": "Smoke test for GET /api/v2/admin/analytics/support. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/analytics/support with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0051",
    "title": "POST api/v2/admin/billing/failed-payments/{invoiceId}/retry",
    "description": "Smoke test for POST /api/v2/admin/billing/failed-payments/{invoiceId}/retry. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/failed-payments/{invoiceId}/retry with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0052",
    "title": "GET api/v2/admin/billing/failed-payments",
    "description": "Smoke test for GET /api/v2/admin/billing/failed-payments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/failed-payments with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0053",
    "title": "POST api/v2/admin/billing/gateways",
    "description": "Smoke test for POST /api/v2/admin/billing/gateways. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/gateways with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0054",
    "title": "POST api/v2/admin/billing/gateways/{gatewayId}/test",
    "description": "Smoke test for POST /api/v2/admin/billing/gateways/{gatewayId}/test. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/gateways/{gatewayId}/test with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0055",
    "title": "GET api/v2/admin/billing/gateways",
    "description": "Smoke test for GET /api/v2/admin/billing/gateways. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/gateways with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0056",
    "title": "GET api/v2/admin/billing/gateways/{gatewayId}",
    "description": "Smoke test for GET /api/v2/admin/billing/gateways/{gatewayId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/gateways/{gatewayId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0057",
    "title": "PUT api/v2/admin/billing/gateways/{gatewayId}",
    "description": "Smoke test for PUT /api/v2/admin/billing/gateways/{gatewayId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/billing/gateways/{gatewayId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0058",
    "title": "DELETE api/v2/admin/billing/gateways/{gatewayId}",
    "description": "Smoke test for DELETE /api/v2/admin/billing/gateways/{gatewayId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/billing/gateways/{gatewayId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0059",
    "title": "POST api/v2/admin/billing/hardware-sales",
    "description": "Smoke test for POST /api/v2/admin/billing/hardware-sales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/hardware-sales with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0060",
    "title": "GET api/v2/admin/billing/hardware-sales",
    "description": "Smoke test for GET /api/v2/admin/billing/hardware-sales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/hardware-sales with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0061",
    "title": "GET api/v2/admin/billing/hardware-sales/{saleId}",
    "description": "Smoke test for GET /api/v2/admin/billing/hardware-sales/{saleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/hardware-sales/{saleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0062",
    "title": "PUT api/v2/admin/billing/hardware-sales/{saleId}",
    "description": "Smoke test for PUT /api/v2/admin/billing/hardware-sales/{saleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/billing/hardware-sales/{saleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0063",
    "title": "DELETE api/v2/admin/billing/hardware-sales/{saleId}",
    "description": "Smoke test for DELETE /api/v2/admin/billing/hardware-sales/{saleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/billing/hardware-sales/{saleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0064",
    "title": "POST api/v2/admin/billing/implementation-fees",
    "description": "Smoke test for POST /api/v2/admin/billing/implementation-fees. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/implementation-fees with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0065",
    "title": "GET api/v2/admin/billing/implementation-fees",
    "description": "Smoke test for GET /api/v2/admin/billing/implementation-fees. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/implementation-fees with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0066",
    "title": "GET api/v2/admin/billing/implementation-fees/{feeId}",
    "description": "Smoke test for GET /api/v2/admin/billing/implementation-fees/{feeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/implementation-fees/{feeId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0067",
    "title": "PUT api/v2/admin/billing/implementation-fees/{feeId}",
    "description": "Smoke test for PUT /api/v2/admin/billing/implementation-fees/{feeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/billing/implementation-fees/{feeId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0068",
    "title": "DELETE api/v2/admin/billing/implementation-fees/{feeId}",
    "description": "Smoke test for DELETE /api/v2/admin/billing/implementation-fees/{feeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/billing/implementation-fees/{feeId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0069",
    "title": "POST api/v2/admin/billing/invoices",
    "description": "Smoke test for POST /api/v2/admin/billing/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/invoices with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0070",
    "title": "POST api/v2/admin/billing/invoices/{invoiceId}/mark-paid",
    "description": "Smoke test for POST /api/v2/admin/billing/invoices/{invoiceId}/mark-paid. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/invoices/{invoiceId}/mark-paid with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0071",
    "title": "POST api/v2/admin/billing/invoices/{invoiceId}/refund",
    "description": "Smoke test for POST /api/v2/admin/billing/invoices/{invoiceId}/refund. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/billing/invoices/{invoiceId}/refund with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0072",
    "title": "GET api/v2/admin/billing/invoices",
    "description": "Smoke test for GET /api/v2/admin/billing/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/invoices with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0073",
    "title": "GET api/v2/admin/billing/invoices/{invoiceId}",
    "description": "Smoke test for GET /api/v2/admin/billing/invoices/{invoiceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/invoices/{invoiceId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0074",
    "title": "GET api/v2/admin/billing/invoices/{invoiceId}/pdf",
    "description": "Smoke test for GET /api/v2/admin/billing/invoices/{invoiceId}/pdf. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/invoices/{invoiceId}/pdf with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0075",
    "title": "GET api/v2/admin/billing/retry-rules",
    "description": "Smoke test for GET /api/v2/admin/billing/retry-rules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/retry-rules with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0076",
    "title": "PUT api/v2/admin/billing/retry-rules",
    "description": "Smoke test for PUT /api/v2/admin/billing/retry-rules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/billing/retry-rules with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0077",
    "title": "GET api/v2/admin/billing/revenue",
    "description": "Smoke test for GET /api/v2/admin/billing/revenue. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/billing/revenue with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0078",
    "title": "POST api/v2/admin/content/announcements",
    "description": "Smoke test for POST /api/v2/admin/content/announcements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/announcements with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0079",
    "title": "GET api/v2/admin/content/announcements",
    "description": "Smoke test for GET /api/v2/admin/content/announcements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/announcements with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0080",
    "title": "GET api/v2/admin/content/announcements/{announcementId}",
    "description": "Smoke test for GET /api/v2/admin/content/announcements/{announcementId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/announcements/{announcementId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0081",
    "title": "PUT api/v2/admin/content/announcements/{announcementId}",
    "description": "Smoke test for PUT /api/v2/admin/content/announcements/{announcementId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/content/announcements/{announcementId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0082",
    "title": "DELETE api/v2/admin/content/announcements/{announcementId}",
    "description": "Smoke test for DELETE /api/v2/admin/content/announcements/{announcementId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/content/announcements/{announcementId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0083",
    "title": "POST api/v2/admin/content/articles",
    "description": "Smoke test for POST /api/v2/admin/content/articles. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/articles with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0084",
    "title": "POST api/v2/admin/content/articles/{articleId}/publish",
    "description": "Smoke test for POST /api/v2/admin/content/articles/{articleId}/publish. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/articles/{articleId}/publish with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0085",
    "title": "GET api/v2/admin/content/articles",
    "description": "Smoke test for GET /api/v2/admin/content/articles. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/articles with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0086",
    "title": "GET api/v2/admin/content/articles/{articleId}",
    "description": "Smoke test for GET /api/v2/admin/content/articles/{articleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/articles/{articleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0087",
    "title": "PUT api/v2/admin/content/articles/{articleId}",
    "description": "Smoke test for PUT /api/v2/admin/content/articles/{articleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/content/articles/{articleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0088",
    "title": "DELETE api/v2/admin/content/articles/{articleId}",
    "description": "Smoke test for DELETE /api/v2/admin/content/articles/{articleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/content/articles/{articleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0089",
    "title": "POST api/v2/admin/content/pages",
    "description": "Smoke test for POST /api/v2/admin/content/pages. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/pages with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0090",
    "title": "POST api/v2/admin/content/pages/{pageId}/publish",
    "description": "Smoke test for POST /api/v2/admin/content/pages/{pageId}/publish. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/pages/{pageId}/publish with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0091",
    "title": "GET api/v2/admin/content/pages",
    "description": "Smoke test for GET /api/v2/admin/content/pages. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/pages with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0092",
    "title": "GET api/v2/admin/content/pages/{pageId}",
    "description": "Smoke test for GET /api/v2/admin/content/pages/{pageId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/pages/{pageId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0093",
    "title": "PUT api/v2/admin/content/pages/{pageId}",
    "description": "Smoke test for PUT /api/v2/admin/content/pages/{pageId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/content/pages/{pageId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0094",
    "title": "DELETE api/v2/admin/content/pages/{pageId}",
    "description": "Smoke test for DELETE /api/v2/admin/content/pages/{pageId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/content/pages/{pageId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0095",
    "title": "POST api/v2/admin/content/templates",
    "description": "Smoke test for POST /api/v2/admin/content/templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/templates with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0096",
    "title": "POST api/v2/admin/content/templates/{templateId}/toggle",
    "description": "Smoke test for POST /api/v2/admin/content/templates/{templateId}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/content/templates/{templateId}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0097",
    "title": "GET api/v2/admin/content/templates",
    "description": "Smoke test for GET /api/v2/admin/content/templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/templates with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0098",
    "title": "GET api/v2/admin/content/templates/{templateId}",
    "description": "Smoke test for GET /api/v2/admin/content/templates/{templateId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/content/templates/{templateId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0099",
    "title": "PUT api/v2/admin/content/templates/{templateId}",
    "description": "Smoke test for PUT /api/v2/admin/content/templates/{templateId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/content/templates/{templateId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0100",
    "title": "DELETE api/v2/admin/content/templates/{templateId}",
    "description": "Smoke test for DELETE /api/v2/admin/content/templates/{templateId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/content/templates/{templateId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0101",
    "title": "GET api/v2/admin/data-management/backup-history",
    "description": "Smoke test for GET /api/v2/admin/data-management/backup-history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/backup-history with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0102",
    "title": "GET api/v2/admin/data-management/backup-history/{itemId}",
    "description": "Smoke test for GET /api/v2/admin/data-management/backup-history/{itemId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/backup-history/{itemId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0103",
    "title": "POST api/v2/admin/data-management/database-backups",
    "description": "Smoke test for POST /api/v2/admin/data-management/database-backups. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/data-management/database-backups with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0104",
    "title": "POST api/v2/admin/data-management/database-backups/{backupId}/complete",
    "description": "Smoke test for POST /api/v2/admin/data-management/database-backups/{backupId}/complete. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/data-management/database-backups/{backupId}/complete with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0105",
    "title": "GET api/v2/admin/data-management/database-backups",
    "description": "Smoke test for GET /api/v2/admin/data-management/database-backups. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/database-backups with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0106",
    "title": "GET api/v2/admin/data-management/database-backups/{backupId}",
    "description": "Smoke test for GET /api/v2/admin/data-management/database-backups/{backupId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/database-backups/{backupId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0107",
    "title": "GET api/v2/admin/data-management/overview",
    "description": "Smoke test for GET /api/v2/admin/data-management/overview. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/overview with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0108",
    "title": "GET api/v2/admin/data-management/provider-backup-statuses",
    "description": "Smoke test for GET /api/v2/admin/data-management/provider-backup-statuses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/provider-backup-statuses with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0109",
    "title": "GET api/v2/admin/data-management/provider-backup-statuses/{statusId}",
    "description": "Smoke test for GET /api/v2/admin/data-management/provider-backup-statuses/{statusId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/provider-backup-statuses/{statusId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0110",
    "title": "POST api/v2/admin/data-management/sync-conflicts/{conflictId}/resolve",
    "description": "Smoke test for POST /api/v2/admin/data-management/sync-conflicts/{conflictId}/resolve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/data-management/sync-conflicts/{conflictId}/resolve with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0111",
    "title": "GET api/v2/admin/data-management/sync-conflicts",
    "description": "Smoke test for GET /api/v2/admin/data-management/sync-conflicts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/sync-conflicts with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0112",
    "title": "GET api/v2/admin/data-management/sync-conflicts/{conflictId}",
    "description": "Smoke test for GET /api/v2/admin/data-management/sync-conflicts/{conflictId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/sync-conflicts/{conflictId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0113",
    "title": "GET api/v2/admin/data-management/sync-logs",
    "description": "Smoke test for GET /api/v2/admin/data-management/sync-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/sync-logs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0114",
    "title": "GET api/v2/admin/data-management/sync-logs/{logId}",
    "description": "Smoke test for GET /api/v2/admin/data-management/sync-logs/{logId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/sync-logs/{logId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0115",
    "title": "GET api/v2/admin/data-management/sync-logs/summary",
    "description": "Smoke test for GET /api/v2/admin/data-management/sync-logs/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/data-management/sync-logs/summary with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0116",
    "title": "GET api/v2/admin/deployment/overview",
    "description": "Smoke test for GET /api/v2/admin/deployment/overview. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/deployment/overview with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0117",
    "title": "POST api/v2/admin/deployment/releases",
    "description": "Smoke test for POST /api/v2/admin/deployment/releases. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/deployment/releases with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0118",
    "title": "POST api/v2/admin/deployment/releases/{releaseId}/activate",
    "description": "Smoke test for POST /api/v2/admin/deployment/releases/{releaseId}/activate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/deployment/releases/{releaseId}/activate with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0119",
    "title": "POST api/v2/admin/deployment/releases/{releaseId}/deactivate",
    "description": "Smoke test for POST /api/v2/admin/deployment/releases/{releaseId}/deactivate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/deployment/releases/{releaseId}/deactivate with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0120",
    "title": "POST api/v2/admin/deployment/releases/{releaseId}/stats",
    "description": "Smoke test for POST /api/v2/admin/deployment/releases/{releaseId}/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/deployment/releases/{releaseId}/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0121",
    "title": "GET api/v2/admin/deployment/releases",
    "description": "Smoke test for GET /api/v2/admin/deployment/releases. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/deployment/releases with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0122",
    "title": "GET api/v2/admin/deployment/releases/{releaseId}",
    "description": "Smoke test for GET /api/v2/admin/deployment/releases/{releaseId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/deployment/releases/{releaseId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0123",
    "title": "GET api/v2/admin/deployment/releases/{releaseId}/stats",
    "description": "Smoke test for GET /api/v2/admin/deployment/releases/{releaseId}/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/deployment/releases/{releaseId}/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0124",
    "title": "GET api/v2/admin/deployment/releases/{releaseId}/summary",
    "description": "Smoke test for GET /api/v2/admin/deployment/releases/{releaseId}/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/deployment/releases/{releaseId}/summary with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0125",
    "title": "PUT api/v2/admin/deployment/releases/{releaseId}",
    "description": "Smoke test for PUT /api/v2/admin/deployment/releases/{releaseId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/deployment/releases/{releaseId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0126",
    "title": "PUT api/v2/admin/deployment/releases/{releaseId}/rollout",
    "description": "Smoke test for PUT /api/v2/admin/deployment/releases/{releaseId}/rollout. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/deployment/releases/{releaseId}/rollout with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0127",
    "title": "DELETE api/v2/admin/deployment/releases/{releaseId}",
    "description": "Smoke test for DELETE /api/v2/admin/deployment/releases/{releaseId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/deployment/releases/{releaseId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0128",
    "title": "POST api/v2/admin/discounts",
    "description": "Smoke test for POST /api/v2/admin/discounts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/discounts with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0129",
    "title": "GET api/v2/admin/discounts",
    "description": "Smoke test for GET /api/v2/admin/discounts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/discounts with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0130",
    "title": "GET api/v2/admin/discounts/{discountId}",
    "description": "Smoke test for GET /api/v2/admin/discounts/{discountId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/discounts/{discountId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0131",
    "title": "PUT api/v2/admin/discounts/{discountId}",
    "description": "Smoke test for PUT /api/v2/admin/discounts/{discountId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/discounts/{discountId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0132",
    "title": "DELETE api/v2/admin/discounts/{discountId}",
    "description": "Smoke test for DELETE /api/v2/admin/discounts/{discountId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/discounts/{discountId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0133",
    "title": "POST api/v2/admin/feature-flags",
    "description": "Smoke test for POST /api/v2/admin/feature-flags. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/feature-flags with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0134",
    "title": "POST api/v2/admin/feature-flags/{flagId}/toggle",
    "description": "Smoke test for POST /api/v2/admin/feature-flags/{flagId}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/feature-flags/{flagId}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0135",
    "title": "GET api/v2/admin/feature-flags",
    "description": "Smoke test for GET /api/v2/admin/feature-flags. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/feature-flags with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0136",
    "title": "GET api/v2/admin/feature-flags/{flagId}",
    "description": "Smoke test for GET /api/v2/admin/feature-flags/{flagId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/feature-flags/{flagId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0137",
    "title": "PUT api/v2/admin/feature-flags/{flagId}",
    "description": "Smoke test for PUT /api/v2/admin/feature-flags/{flagId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/feature-flags/{flagId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0138",
    "title": "DELETE api/v2/admin/feature-flags/{flagId}",
    "description": "Smoke test for DELETE /api/v2/admin/feature-flags/{flagId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/feature-flags/{flagId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0139",
    "title": "GET api/v2/admin/feature-flags/stats",
    "description": "Smoke test for GET /api/v2/admin/feature-flags/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/feature-flags/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0140",
    "title": "POST api/v2/admin/financial-operations/account-mappings",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/account-mappings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/account-mappings with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0141",
    "title": "GET api/v2/admin/financial-operations/account-mappings",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/account-mappings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/account-mappings with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0142",
    "title": "GET api/v2/admin/financial-operations/account-mappings/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/account-mappings/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/account-mappings/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0143",
    "title": "PUT api/v2/admin/financial-operations/account-mappings/{id}",
    "description": "Smoke test for PUT /api/v2/admin/financial-operations/account-mappings/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/financial-operations/account-mappings/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0144",
    "title": "DELETE api/v2/admin/financial-operations/account-mappings/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/financial-operations/account-mappings/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/financial-operations/account-mappings/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0145",
    "title": "POST api/v2/admin/financial-operations/accounting-configs",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/accounting-configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/accounting-configs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0146",
    "title": "GET api/v2/admin/financial-operations/accounting-configs",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/accounting-configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/accounting-configs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0147",
    "title": "GET api/v2/admin/financial-operations/accounting-configs/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/accounting-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/accounting-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0148",
    "title": "PUT api/v2/admin/financial-operations/accounting-configs/{id}",
    "description": "Smoke test for PUT /api/v2/admin/financial-operations/accounting-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/financial-operations/accounting-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0149",
    "title": "DELETE api/v2/admin/financial-operations/accounting-configs/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/financial-operations/accounting-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/financial-operations/accounting-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0150",
    "title": "POST api/v2/admin/financial-operations/accounting-exports",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/accounting-exports. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/accounting-exports with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0151",
    "title": "POST api/v2/admin/financial-operations/accounting-exports/{id}/retry",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/accounting-exports/{id}/retry. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/accounting-exports/{id}/retry with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0152",
    "title": "GET api/v2/admin/financial-operations/accounting-exports",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/accounting-exports. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/accounting-exports with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0153",
    "title": "GET api/v2/admin/financial-operations/accounting-exports/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/accounting-exports/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/accounting-exports/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0154",
    "title": "POST api/v2/admin/financial-operations/auto-export-configs",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/auto-export-configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/auto-export-configs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0155",
    "title": "GET api/v2/admin/financial-operations/auto-export-configs",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/auto-export-configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/auto-export-configs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0156",
    "title": "GET api/v2/admin/financial-operations/auto-export-configs/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/auto-export-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/auto-export-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0157",
    "title": "PUT api/v2/admin/financial-operations/auto-export-configs/{id}",
    "description": "Smoke test for PUT /api/v2/admin/financial-operations/auto-export-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/financial-operations/auto-export-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0158",
    "title": "DELETE api/v2/admin/financial-operations/auto-export-configs/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/financial-operations/auto-export-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/financial-operations/auto-export-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0159",
    "title": "GET api/v2/admin/financial-operations/cash-events",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/cash-events. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/cash-events with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0160",
    "title": "GET api/v2/admin/financial-operations/cash-events/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/cash-events/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/cash-events/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0161",
    "title": "POST api/v2/admin/financial-operations/cash-sessions/{id}/force-close",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/cash-sessions/{id}/force-close. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/cash-sessions/{id}/force-close with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0162",
    "title": "GET api/v2/admin/financial-operations/cash-sessions",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/cash-sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/cash-sessions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0163",
    "title": "GET api/v2/admin/financial-operations/cash-sessions/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/cash-sessions/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/cash-sessions/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0164",
    "title": "GET api/v2/admin/financial-operations/daily-sales-summary",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/daily-sales-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/daily-sales-summary with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0165",
    "title": "GET api/v2/admin/financial-operations/daily-sales-summary/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/daily-sales-summary/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/daily-sales-summary/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0166",
    "title": "POST api/v2/admin/financial-operations/expenses",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/expenses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/expenses with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0167",
    "title": "GET api/v2/admin/financial-operations/expenses",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/expenses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/expenses with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0168",
    "title": "GET api/v2/admin/financial-operations/expenses/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/expenses/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/expenses/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0169",
    "title": "PUT api/v2/admin/financial-operations/expenses/{id}",
    "description": "Smoke test for PUT /api/v2/admin/financial-operations/expenses/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/financial-operations/expenses/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0170",
    "title": "DELETE api/v2/admin/financial-operations/expenses/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/financial-operations/expenses/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/financial-operations/expenses/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0171",
    "title": "GET api/v2/admin/financial-operations/gift-card-transactions",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/gift-card-transactions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/gift-card-transactions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0172",
    "title": "GET api/v2/admin/financial-operations/gift-card-transactions/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/gift-card-transactions/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/gift-card-transactions/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0173",
    "title": "POST api/v2/admin/financial-operations/gift-cards",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/gift-cards. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/gift-cards with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0174",
    "title": "POST api/v2/admin/financial-operations/gift-cards/{id}/void",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/gift-cards/{id}/void. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/gift-cards/{id}/void with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0175",
    "title": "GET api/v2/admin/financial-operations/gift-cards",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/gift-cards. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/gift-cards with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0176",
    "title": "GET api/v2/admin/financial-operations/gift-cards/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/gift-cards/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/gift-cards/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0177",
    "title": "PUT api/v2/admin/financial-operations/gift-cards/{id}",
    "description": "Smoke test for PUT /api/v2/admin/financial-operations/gift-cards/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/financial-operations/gift-cards/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0178",
    "title": "GET api/v2/admin/financial-operations/overview",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/overview. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/overview with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0179",
    "title": "GET api/v2/admin/financial-operations/payments",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/payments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/payments with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0180",
    "title": "GET api/v2/admin/financial-operations/payments/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/payments/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/payments/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0181",
    "title": "GET api/v2/admin/financial-operations/product-sales-summary",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/product-sales-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/product-sales-summary with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0182",
    "title": "GET api/v2/admin/financial-operations/product-sales-summary/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/product-sales-summary/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/product-sales-summary/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0183",
    "title": "POST api/v2/admin/financial-operations/refunds/{id}/process",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/refunds/{id}/process. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/refunds/{id}/process with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0184",
    "title": "GET api/v2/admin/financial-operations/refunds",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/refunds. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/refunds with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0185",
    "title": "GET api/v2/admin/financial-operations/refunds/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/refunds/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/refunds/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0186",
    "title": "GET api/v2/admin/financial-operations/thawani-orders",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/thawani-orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/thawani-orders with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0187",
    "title": "GET api/v2/admin/financial-operations/thawani-orders/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/thawani-orders/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/thawani-orders/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0188",
    "title": "POST api/v2/admin/financial-operations/thawani-settlements/{id}/reconcile",
    "description": "Smoke test for POST /api/v2/admin/financial-operations/thawani-settlements/{id}/reconcile. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/financial-operations/thawani-settlements/{id}/reconcile with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0189",
    "title": "GET api/v2/admin/financial-operations/thawani-settlements",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/thawani-settlements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/thawani-settlements with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0190",
    "title": "GET api/v2/admin/financial-operations/thawani-settlements/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/thawani-settlements/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/thawani-settlements/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0191",
    "title": "GET api/v2/admin/financial-operations/thawani-store-configs",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/thawani-store-configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/thawani-store-configs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0192",
    "title": "GET api/v2/admin/financial-operations/thawani-store-configs/{id}",
    "description": "Smoke test for GET /api/v2/admin/financial-operations/thawani-store-configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/financial-operations/thawani-store-configs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0193",
    "title": "POST api/v2/admin/infrastructure/cache/flush",
    "description": "Smoke test for POST /api/v2/admin/infrastructure/cache/flush. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/infrastructure/cache/flush with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0194",
    "title": "GET api/v2/admin/infrastructure/cache/stats",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/cache/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/cache/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0195",
    "title": "GET api/v2/admin/infrastructure/database-backups",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/database-backups. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/database-backups with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0196",
    "title": "GET api/v2/admin/infrastructure/database-backups/{id}",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/database-backups/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/database-backups/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0197",
    "title": "POST api/v2/admin/infrastructure/failed-jobs/{id}/retry",
    "description": "Smoke test for POST /api/v2/admin/infrastructure/failed-jobs/{id}/retry. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/infrastructure/failed-jobs/{id}/retry with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0198",
    "title": "GET api/v2/admin/infrastructure/failed-jobs",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/failed-jobs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/failed-jobs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0199",
    "title": "GET api/v2/admin/infrastructure/failed-jobs/{id}",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/failed-jobs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/failed-jobs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0200",
    "title": "DELETE api/v2/admin/infrastructure/failed-jobs/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/infrastructure/failed-jobs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/infrastructure/failed-jobs/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0201",
    "title": "GET api/v2/admin/infrastructure/health-checks",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/health-checks. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/health-checks with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0202",
    "title": "GET api/v2/admin/infrastructure/health-checks/{id}",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/health-checks/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/health-checks/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0203",
    "title": "GET api/v2/admin/infrastructure/overview",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/overview. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/overview with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0204",
    "title": "GET api/v2/admin/infrastructure/provider-backups",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/provider-backups. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/provider-backups with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0205",
    "title": "GET api/v2/admin/infrastructure/provider-backups/{id}",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/provider-backups/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/provider-backups/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0206",
    "title": "GET api/v2/admin/infrastructure/server-metrics",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/server-metrics. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/server-metrics with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0207",
    "title": "GET api/v2/admin/infrastructure/storage-usage",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/storage-usage. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/storage-usage with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0208",
    "title": "GET api/v2/admin/infrastructure/system-settings",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/system-settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/system-settings with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0209",
    "title": "GET api/v2/admin/infrastructure/system-settings/{id}",
    "description": "Smoke test for GET /api/v2/admin/infrastructure/system-settings/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/infrastructure/system-settings/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0210",
    "title": "POST api/v2/admin/installment-providers/{id}/maintenance",
    "description": "Smoke test for POST /api/v2/admin/installment-providers/{id}/maintenance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/installment-providers/{id}/maintenance with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0211",
    "title": "POST api/v2/admin/installment-providers/{id}/toggle",
    "description": "Smoke test for POST /api/v2/admin/installment-providers/{id}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/installment-providers/{id}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0212",
    "title": "GET api/v2/admin/installment-providers",
    "description": "Smoke test for GET /api/v2/admin/installment-providers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/installment-providers with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0213",
    "title": "GET api/v2/admin/installment-providers/{id}",
    "description": "Smoke test for GET /api/v2/admin/installment-providers/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/installment-providers/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0214",
    "title": "PUT api/v2/admin/installment-providers/{id}",
    "description": "Smoke test for PUT /api/v2/admin/installment-providers/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/installment-providers/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0215",
    "title": "GET api/v2/admin/invoices",
    "description": "Smoke test for GET /api/v2/admin/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/invoices with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0216",
    "title": "GET api/v2/admin/invoices/{invoiceId}",
    "description": "Smoke test for GET /api/v2/admin/invoices/{invoiceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/invoices/{invoiceId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0217",
    "title": "GET api/v2/admin/logs/activity",
    "description": "Smoke test for GET /api/v2/admin/logs/activity. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/activity with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0218",
    "title": "GET api/v2/admin/logs/activity/{logId}",
    "description": "Smoke test for GET /api/v2/admin/logs/activity/{logId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/activity/{logId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0219",
    "title": "POST api/v2/admin/logs/events",
    "description": "Smoke test for POST /api/v2/admin/logs/events. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/logs/events with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0220",
    "title": "GET api/v2/admin/logs/events",
    "description": "Smoke test for GET /api/v2/admin/logs/events. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/events with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0221",
    "title": "GET api/v2/admin/logs/events/{eventId}",
    "description": "Smoke test for GET /api/v2/admin/logs/events/{eventId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/events/{eventId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0222",
    "title": "POST api/v2/admin/logs/health/checks",
    "description": "Smoke test for POST /api/v2/admin/logs/health/checks. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/logs/health/checks with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0223",
    "title": "GET api/v2/admin/logs/health/checks",
    "description": "Smoke test for GET /api/v2/admin/logs/health/checks. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/health/checks with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0224",
    "title": "GET api/v2/admin/logs/health/dashboard",
    "description": "Smoke test for GET /api/v2/admin/logs/health/dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/health/dashboard with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0225",
    "title": "GET api/v2/admin/logs/notifications",
    "description": "Smoke test for GET /api/v2/admin/logs/notifications. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/notifications with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0226",
    "title": "POST api/v2/admin/logs/security-alerts/{alertId}/resolve",
    "description": "Smoke test for POST /api/v2/admin/logs/security-alerts/{alertId}/resolve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/logs/security-alerts/{alertId}/resolve with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0227",
    "title": "GET api/v2/admin/logs/security-alerts",
    "description": "Smoke test for GET /api/v2/admin/logs/security-alerts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/security-alerts with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0228",
    "title": "GET api/v2/admin/logs/security-alerts/{alertId}",
    "description": "Smoke test for GET /api/v2/admin/logs/security-alerts/{alertId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/security-alerts/{alertId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0229",
    "title": "GET api/v2/admin/logs/stats",
    "description": "Smoke test for GET /api/v2/admin/logs/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0230",
    "title": "GET api/v2/admin/logs/store-health",
    "description": "Smoke test for GET /api/v2/admin/logs/store-health. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/logs/store-health with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0231",
    "title": "GET api/v2/admin/marketplace/orders",
    "description": "Smoke test for GET /api/v2/admin/marketplace/orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/orders with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0232",
    "title": "GET api/v2/admin/marketplace/orders/{orderId}",
    "description": "Smoke test for GET /api/v2/admin/marketplace/orders/{orderId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/orders/{orderId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0233",
    "title": "GET api/v2/admin/marketplace/products",
    "description": "Smoke test for GET /api/v2/admin/marketplace/products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/products with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0234",
    "title": "GET api/v2/admin/marketplace/products/{mappingId}",
    "description": "Smoke test for GET /api/v2/admin/marketplace/products/{mappingId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/products/{mappingId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0235",
    "title": "PUT api/v2/admin/marketplace/products/{mappingId}",
    "description": "Smoke test for PUT /api/v2/admin/marketplace/products/{mappingId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/marketplace/products/{mappingId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0236",
    "title": "POST api/v2/admin/marketplace/products/bulk-publish",
    "description": "Smoke test for POST /api/v2/admin/marketplace/products/bulk-publish. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/marketplace/products/bulk-publish with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0237",
    "title": "GET api/v2/admin/marketplace/settlements",
    "description": "Smoke test for GET /api/v2/admin/marketplace/settlements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/settlements with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0238",
    "title": "GET api/v2/admin/marketplace/settlements/{settlementId}",
    "description": "Smoke test for GET /api/v2/admin/marketplace/settlements/{settlementId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/settlements/{settlementId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0239",
    "title": "GET api/v2/admin/marketplace/settlements/summary",
    "description": "Smoke test for GET /api/v2/admin/marketplace/settlements/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/settlements/summary with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0240",
    "title": "POST api/v2/admin/marketplace/stores/{configId}/disconnect",
    "description": "Smoke test for POST /api/v2/admin/marketplace/stores/{configId}/disconnect. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/marketplace/stores/{configId}/disconnect with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0241",
    "title": "POST api/v2/admin/marketplace/stores/{storeId}/connect",
    "description": "Smoke test for POST /api/v2/admin/marketplace/stores/{storeId}/connect. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/marketplace/stores/{storeId}/connect with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0242",
    "title": "GET api/v2/admin/marketplace/stores",
    "description": "Smoke test for GET /api/v2/admin/marketplace/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/stores with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0243",
    "title": "GET api/v2/admin/marketplace/stores/{configId}",
    "description": "Smoke test for GET /api/v2/admin/marketplace/stores/{configId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/marketplace/stores/{configId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0244",
    "title": "PUT api/v2/admin/marketplace/stores/{configId}",
    "description": "Smoke test for PUT /api/v2/admin/marketplace/stores/{configId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/marketplace/stores/{configId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0245",
    "title": "GET api/v2/admin/me",
    "description": "Smoke test for GET /api/v2/admin/me. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/me with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0246",
    "title": "GET api/v2/admin/permissions",
    "description": "Smoke test for GET /api/v2/admin/permissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/permissions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0247",
    "title": "POST api/v2/admin/plans",
    "description": "Smoke test for POST /api/v2/admin/plans. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/plans with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0248",
    "title": "POST api/v2/admin/plans/{planId}/toggle",
    "description": "Smoke test for POST /api/v2/admin/plans/{planId}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/plans/{planId}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0249",
    "title": "GET api/v2/admin/plans",
    "description": "Smoke test for GET /api/v2/admin/plans. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/plans with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0250",
    "title": "GET api/v2/admin/plans/{planId}",
    "description": "Smoke test for GET /api/v2/admin/plans/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/plans/{planId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0251",
    "title": "PUT api/v2/admin/plans/{planId}",
    "description": "Smoke test for PUT /api/v2/admin/plans/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/plans/{planId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0252",
    "title": "DELETE api/v2/admin/plans/{planId}",
    "description": "Smoke test for DELETE /api/v2/admin/plans/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/plans/{planId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0253",
    "title": "GET api/v2/admin/plans/compare",
    "description": "Smoke test for GET /api/v2/admin/plans/compare. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/plans/compare with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0254",
    "title": "GET api/v2/admin/provider-roles/permissions",
    "description": "Smoke test for GET /api/v2/admin/provider-roles/permissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/provider-roles/permissions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0255",
    "title": "POST api/v2/admin/provider-roles/templates",
    "description": "Smoke test for POST /api/v2/admin/provider-roles/templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/provider-roles/templates with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0256",
    "title": "GET api/v2/admin/provider-roles/templates",
    "description": "Smoke test for GET /api/v2/admin/provider-roles/templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/provider-roles/templates with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0257",
    "title": "GET api/v2/admin/provider-roles/templates/{id}",
    "description": "Smoke test for GET /api/v2/admin/provider-roles/templates/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/provider-roles/templates/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0258",
    "title": "GET api/v2/admin/provider-roles/templates/{id}/permissions",
    "description": "Smoke test for GET /api/v2/admin/provider-roles/templates/{id}/permissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/provider-roles/templates/{id}/permissions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0259",
    "title": "PUT api/v2/admin/provider-roles/templates/{id}",
    "description": "Smoke test for PUT /api/v2/admin/provider-roles/templates/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/provider-roles/templates/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0260",
    "title": "PUT api/v2/admin/provider-roles/templates/{id}/permissions",
    "description": "Smoke test for PUT /api/v2/admin/provider-roles/templates/{id}/permissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/provider-roles/templates/{id}/permissions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0261",
    "title": "DELETE api/v2/admin/provider-roles/templates/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/provider-roles/templates/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/provider-roles/templates/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0262",
    "title": "POST api/v2/admin/providers/notes",
    "description": "Smoke test for POST /api/v2/admin/providers/notes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/notes with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0263",
    "title": "GET api/v2/admin/providers/notes/{organizationId}",
    "description": "Smoke test for GET /api/v2/admin/providers/notes/{organizationId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/providers/notes/{organizationId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0264",
    "title": "POST api/v2/admin/providers/registrations/{registrationId}/approve",
    "description": "Smoke test for POST /api/v2/admin/providers/registrations/{registrationId}/approve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/registrations/{registrationId}/approve with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0265",
    "title": "POST api/v2/admin/providers/registrations/{registrationId}/reject",
    "description": "Smoke test for POST /api/v2/admin/providers/registrations/{registrationId}/reject. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/registrations/{registrationId}/reject with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0266",
    "title": "GET api/v2/admin/providers/registrations",
    "description": "Smoke test for GET /api/v2/admin/providers/registrations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/providers/registrations with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0267",
    "title": "POST api/v2/admin/providers/stores/{storeId}/activate",
    "description": "Smoke test for POST /api/v2/admin/providers/stores/{storeId}/activate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/stores/{storeId}/activate with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0268",
    "title": "POST api/v2/admin/providers/stores/{storeId}/limits",
    "description": "Smoke test for POST /api/v2/admin/providers/stores/{storeId}/limits. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/stores/{storeId}/limits with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0269",
    "title": "POST api/v2/admin/providers/stores/{storeId}/suspend",
    "description": "Smoke test for POST /api/v2/admin/providers/stores/{storeId}/suspend. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/stores/{storeId}/suspend with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0270",
    "title": "GET api/v2/admin/providers/stores",
    "description": "Smoke test for GET /api/v2/admin/providers/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/providers/stores with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0271",
    "title": "GET api/v2/admin/providers/stores/{storeId}",
    "description": "Smoke test for GET /api/v2/admin/providers/stores/{storeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/providers/stores/{storeId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0272",
    "title": "GET api/v2/admin/providers/stores/{storeId}/limits",
    "description": "Smoke test for GET /api/v2/admin/providers/stores/{storeId}/limits. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/providers/stores/{storeId}/limits with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0273",
    "title": "GET api/v2/admin/providers/stores/{storeId}/metrics",
    "description": "Smoke test for GET /api/v2/admin/providers/stores/{storeId}/metrics. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/providers/stores/{storeId}/metrics with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0274",
    "title": "DELETE api/v2/admin/providers/stores/{storeId}/limits/{limitKey}",
    "description": "Smoke test for DELETE /api/v2/admin/providers/stores/{storeId}/limits/{limitKey}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/providers/stores/{storeId}/limits/{limitKey} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0275",
    "title": "POST api/v2/admin/providers/stores/create",
    "description": "Smoke test for POST /api/v2/admin/providers/stores/create. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/stores/create with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0276",
    "title": "POST api/v2/admin/providers/stores/export",
    "description": "Smoke test for POST /api/v2/admin/providers/stores/export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/providers/stores/export with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0277",
    "title": "GET api/v2/admin/revenue-dashboard",
    "description": "Smoke test for GET /api/v2/admin/revenue-dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/revenue-dashboard with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0278",
    "title": "POST api/v2/admin/roles",
    "description": "Smoke test for POST /api/v2/admin/roles. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/roles with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0279",
    "title": "GET api/v2/admin/roles",
    "description": "Smoke test for GET /api/v2/admin/roles. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/roles with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0280",
    "title": "GET api/v2/admin/roles/{roleId}",
    "description": "Smoke test for GET /api/v2/admin/roles/{roleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/roles/{roleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0281",
    "title": "PUT api/v2/admin/roles/{roleId}",
    "description": "Smoke test for PUT /api/v2/admin/roles/{roleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/roles/{roleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0282",
    "title": "DELETE api/v2/admin/roles/{roleId}",
    "description": "Smoke test for DELETE /api/v2/admin/roles/{roleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/roles/{roleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0283",
    "title": "POST api/v2/admin/security-center/alerts/{alertId}/resolve",
    "description": "Smoke test for POST /api/v2/admin/security-center/alerts/{alertId}/resolve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/security-center/alerts/{alertId}/resolve with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0284",
    "title": "GET api/v2/admin/security-center/alerts",
    "description": "Smoke test for GET /api/v2/admin/security-center/alerts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/alerts with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0285",
    "title": "GET api/v2/admin/security-center/alerts/{alertId}",
    "description": "Smoke test for GET /api/v2/admin/security-center/alerts/{alertId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/alerts/{alertId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0286",
    "title": "GET api/v2/admin/security-center/audit-logs",
    "description": "Smoke test for GET /api/v2/admin/security-center/audit-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/audit-logs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0287",
    "title": "GET api/v2/admin/security-center/audit-logs/{logId}",
    "description": "Smoke test for GET /api/v2/admin/security-center/audit-logs/{logId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/audit-logs/{logId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0288",
    "title": "POST api/v2/admin/security-center/devices/{deviceId}/wipe",
    "description": "Smoke test for POST /api/v2/admin/security-center/devices/{deviceId}/wipe. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/security-center/devices/{deviceId}/wipe with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0289",
    "title": "GET api/v2/admin/security-center/devices",
    "description": "Smoke test for GET /api/v2/admin/security-center/devices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/devices with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0290",
    "title": "GET api/v2/admin/security-center/devices/{deviceId}",
    "description": "Smoke test for GET /api/v2/admin/security-center/devices/{deviceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/devices/{deviceId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0291",
    "title": "POST api/v2/admin/security-center/ip-allowlist",
    "description": "Smoke test for POST /api/v2/admin/security-center/ip-allowlist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/security-center/ip-allowlist with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0292",
    "title": "GET api/v2/admin/security-center/ip-allowlist",
    "description": "Smoke test for GET /api/v2/admin/security-center/ip-allowlist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/ip-allowlist with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0293",
    "title": "DELETE api/v2/admin/security-center/ip-allowlist/{entryId}",
    "description": "Smoke test for DELETE /api/v2/admin/security-center/ip-allowlist/{entryId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/security-center/ip-allowlist/{entryId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0294",
    "title": "POST api/v2/admin/security-center/ip-blocklist",
    "description": "Smoke test for POST /api/v2/admin/security-center/ip-blocklist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/security-center/ip-blocklist with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0295",
    "title": "GET api/v2/admin/security-center/ip-blocklist",
    "description": "Smoke test for GET /api/v2/admin/security-center/ip-blocklist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/ip-blocklist with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0296",
    "title": "DELETE api/v2/admin/security-center/ip-blocklist/{entryId}",
    "description": "Smoke test for DELETE /api/v2/admin/security-center/ip-blocklist/{entryId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/security-center/ip-blocklist/{entryId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0297",
    "title": "GET api/v2/admin/security-center/login-attempts",
    "description": "Smoke test for GET /api/v2/admin/security-center/login-attempts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/login-attempts with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0298",
    "title": "GET api/v2/admin/security-center/login-attempts/{attemptId}",
    "description": "Smoke test for GET /api/v2/admin/security-center/login-attempts/{attemptId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/login-attempts/{attemptId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0299",
    "title": "GET api/v2/admin/security-center/overview",
    "description": "Smoke test for GET /api/v2/admin/security-center/overview. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/overview with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0300",
    "title": "GET api/v2/admin/security-center/policies",
    "description": "Smoke test for GET /api/v2/admin/security-center/policies. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/policies with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0301",
    "title": "GET api/v2/admin/security-center/policies/{policyId}",
    "description": "Smoke test for GET /api/v2/admin/security-center/policies/{policyId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/policies/{policyId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0302",
    "title": "PUT api/v2/admin/security-center/policies/{policyId}",
    "description": "Smoke test for PUT /api/v2/admin/security-center/policies/{policyId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/security-center/policies/{policyId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0303",
    "title": "POST api/v2/admin/security-center/sessions/{sessionId}/revoke",
    "description": "Smoke test for POST /api/v2/admin/security-center/sessions/{sessionId}/revoke. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/security-center/sessions/{sessionId}/revoke with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0304",
    "title": "GET api/v2/admin/security-center/sessions",
    "description": "Smoke test for GET /api/v2/admin/security-center/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/sessions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0305",
    "title": "GET api/v2/admin/security-center/sessions/{sessionId}",
    "description": "Smoke test for GET /api/v2/admin/security-center/sessions/{sessionId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/security-center/sessions/{sessionId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0306",
    "title": "GET api/v2/admin/subscriptions",
    "description": "Smoke test for GET /api/v2/admin/subscriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/subscriptions with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0307",
    "title": "GET api/v2/admin/subscriptions/{subscriptionId}",
    "description": "Smoke test for GET /api/v2/admin/subscriptions/{subscriptionId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/subscriptions/{subscriptionId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0308",
    "title": "POST api/v2/admin/support/canned-responses",
    "description": "Smoke test for POST /api/v2/admin/support/canned-responses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/canned-responses with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0309",
    "title": "POST api/v2/admin/support/canned-responses/{responseId}/toggle",
    "description": "Smoke test for POST /api/v2/admin/support/canned-responses/{responseId}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/canned-responses/{responseId}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0310",
    "title": "GET api/v2/admin/support/canned-responses",
    "description": "Smoke test for GET /api/v2/admin/support/canned-responses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/canned-responses with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0311",
    "title": "GET api/v2/admin/support/canned-responses/{responseId}",
    "description": "Smoke test for GET /api/v2/admin/support/canned-responses/{responseId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/canned-responses/{responseId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0312",
    "title": "PUT api/v2/admin/support/canned-responses/{responseId}",
    "description": "Smoke test for PUT /api/v2/admin/support/canned-responses/{responseId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/support/canned-responses/{responseId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0313",
    "title": "DELETE api/v2/admin/support/canned-responses/{responseId}",
    "description": "Smoke test for DELETE /api/v2/admin/support/canned-responses/{responseId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/support/canned-responses/{responseId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0314",
    "title": "POST api/v2/admin/support/kb",
    "description": "Smoke test for POST /api/v2/admin/support/kb. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/kb with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0315",
    "title": "GET api/v2/admin/support/kb",
    "description": "Smoke test for GET /api/v2/admin/support/kb. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/kb with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0316",
    "title": "GET api/v2/admin/support/kb/{articleId}",
    "description": "Smoke test for GET /api/v2/admin/support/kb/{articleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/kb/{articleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0317",
    "title": "PUT api/v2/admin/support/kb/{articleId}",
    "description": "Smoke test for PUT /api/v2/admin/support/kb/{articleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/support/kb/{articleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0318",
    "title": "DELETE api/v2/admin/support/kb/{articleId}",
    "description": "Smoke test for DELETE /api/v2/admin/support/kb/{articleId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/support/kb/{articleId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0319",
    "title": "GET api/v2/admin/support/stats",
    "description": "Smoke test for GET /api/v2/admin/support/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0320",
    "title": "POST api/v2/admin/support/tickets",
    "description": "Smoke test for POST /api/v2/admin/support/tickets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/tickets with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0321",
    "title": "POST api/v2/admin/support/tickets/{ticketId}/assign",
    "description": "Smoke test for POST /api/v2/admin/support/tickets/{ticketId}/assign. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/tickets/{ticketId}/assign with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0322",
    "title": "POST api/v2/admin/support/tickets/{ticketId}/messages",
    "description": "Smoke test for POST /api/v2/admin/support/tickets/{ticketId}/messages. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/tickets/{ticketId}/messages with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0323",
    "title": "POST api/v2/admin/support/tickets/{ticketId}/status",
    "description": "Smoke test for POST /api/v2/admin/support/tickets/{ticketId}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/support/tickets/{ticketId}/status with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0324",
    "title": "GET api/v2/admin/support/tickets",
    "description": "Smoke test for GET /api/v2/admin/support/tickets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/tickets with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0325",
    "title": "GET api/v2/admin/support/tickets/{ticketId}",
    "description": "Smoke test for GET /api/v2/admin/support/tickets/{ticketId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/tickets/{ticketId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0326",
    "title": "GET api/v2/admin/support/tickets/{ticketId}/messages",
    "description": "Smoke test for GET /api/v2/admin/support/tickets/{ticketId}/messages. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/support/tickets/{ticketId}/messages with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0327",
    "title": "PUT api/v2/admin/support/tickets/{ticketId}",
    "description": "Smoke test for PUT /api/v2/admin/support/tickets/{ticketId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/support/tickets/{ticketId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0328",
    "title": "POST api/v2/admin/team",
    "description": "Smoke test for POST /api/v2/admin/team. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/team with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0329",
    "title": "POST api/v2/admin/team/{userId}/activate",
    "description": "Smoke test for POST /api/v2/admin/team/{userId}/activate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/team/{userId}/activate with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0330",
    "title": "POST api/v2/admin/team/{userId}/deactivate",
    "description": "Smoke test for POST /api/v2/admin/team/{userId}/deactivate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/team/{userId}/deactivate with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0331",
    "title": "GET api/v2/admin/team",
    "description": "Smoke test for GET /api/v2/admin/team. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/team with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0332",
    "title": "GET api/v2/admin/team/{userId}",
    "description": "Smoke test for GET /api/v2/admin/team/{userId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/team/{userId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0333",
    "title": "PUT api/v2/admin/team/{userId}",
    "description": "Smoke test for PUT /api/v2/admin/team/{userId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/team/{userId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0334",
    "title": "POST api/v2/admin/terminals",
    "description": "Smoke test for POST /api/v2/admin/terminals. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/terminals with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0335",
    "title": "POST api/v2/admin/terminals/{register}/activate-softpos",
    "description": "Smoke test for POST /api/v2/admin/terminals/{register}/activate-softpos. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/terminals/{register}/activate-softpos with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0336",
    "title": "POST api/v2/admin/terminals/{register}/deactivate-softpos",
    "description": "Smoke test for POST /api/v2/admin/terminals/{register}/deactivate-softpos. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/terminals/{register}/deactivate-softpos with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0337",
    "title": "POST api/v2/admin/terminals/{register}/suspend-softpos",
    "description": "Smoke test for POST /api/v2/admin/terminals/{register}/suspend-softpos. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/terminals/{register}/suspend-softpos with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0338",
    "title": "POST api/v2/admin/terminals/{register}/toggle-status",
    "description": "Smoke test for POST /api/v2/admin/terminals/{register}/toggle-status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/terminals/{register}/toggle-status with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0339",
    "title": "GET api/v2/admin/terminals",
    "description": "Smoke test for GET /api/v2/admin/terminals. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/terminals with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0340",
    "title": "GET api/v2/admin/terminals/{register}",
    "description": "Smoke test for GET /api/v2/admin/terminals/{register}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/terminals/{register} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0341",
    "title": "PUT api/v2/admin/terminals/{register}",
    "description": "Smoke test for PUT /api/v2/admin/terminals/{register}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/terminals/{register} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0342",
    "title": "PUT api/v2/admin/terminals/{register}/fees",
    "description": "Smoke test for PUT /api/v2/admin/terminals/{register}/fees. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/terminals/{register}/fees with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0343",
    "title": "DELETE api/v2/admin/terminals/{register}",
    "description": "Smoke test for DELETE /api/v2/admin/terminals/{register}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/terminals/{register} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0344",
    "title": "GET api/v2/admin/terminals/stats",
    "description": "Smoke test for GET /api/v2/admin/terminals/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/terminals/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0345",
    "title": "POST api/v2/admin/users/admins",
    "description": "Smoke test for POST /api/v2/admin/users/admins. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/users/admins with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0346",
    "title": "POST api/v2/admin/users/admins/{userId}/reset-2fa",
    "description": "Smoke test for POST /api/v2/admin/users/admins/{userId}/reset-2fa. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/users/admins/{userId}/reset-2fa with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0347",
    "title": "GET api/v2/admin/users/admins",
    "description": "Smoke test for GET /api/v2/admin/users/admins. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/admins with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0348",
    "title": "GET api/v2/admin/users/admins/{userId}",
    "description": "Smoke test for GET /api/v2/admin/users/admins/{userId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/admins/{userId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0349",
    "title": "GET api/v2/admin/users/admins/{userId}/activity",
    "description": "Smoke test for GET /api/v2/admin/users/admins/{userId}/activity. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/admins/{userId}/activity with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0350",
    "title": "PUT api/v2/admin/users/admins/{userId}",
    "description": "Smoke test for PUT /api/v2/admin/users/admins/{userId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/users/admins/{userId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0351",
    "title": "POST api/v2/admin/users/provider/{userId}/force-password-change",
    "description": "Smoke test for POST /api/v2/admin/users/provider/{userId}/force-password-change. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/users/provider/{userId}/force-password-change with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0352",
    "title": "POST api/v2/admin/users/provider/{userId}/reset-password",
    "description": "Smoke test for POST /api/v2/admin/users/provider/{userId}/reset-password. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/users/provider/{userId}/reset-password with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0353",
    "title": "POST api/v2/admin/users/provider/{userId}/toggle-active",
    "description": "Smoke test for POST /api/v2/admin/users/provider/{userId}/toggle-active. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/users/provider/{userId}/toggle-active with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0354",
    "title": "GET api/v2/admin/users/provider",
    "description": "Smoke test for GET /api/v2/admin/users/provider. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/provider with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0355",
    "title": "GET api/v2/admin/users/provider/{userId}",
    "description": "Smoke test for GET /api/v2/admin/users/provider/{userId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/provider/{userId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0356",
    "title": "GET api/v2/admin/users/provider/{userId}/activity",
    "description": "Smoke test for GET /api/v2/admin/users/provider/{userId}/activity. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/provider/{userId}/activity with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0357",
    "title": "GET api/v2/admin/users/stats",
    "description": "Smoke test for GET /api/v2/admin/users/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/users/stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0358",
    "title": "GET api/v2/admin/wameed-ai/analytics/chats",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/analytics/chats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/analytics/chats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0359",
    "title": "GET api/v2/admin/wameed-ai/analytics/chats/{chatId}",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/analytics/chats/{chatId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/analytics/chats/{chatId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0360",
    "title": "GET api/v2/admin/wameed-ai/analytics/dashboard",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/analytics/dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/analytics/dashboard with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0361",
    "title": "POST api/v2/admin/wameed-ai/billing/check-overdue",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/billing/check-overdue. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/billing/check-overdue with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0362",
    "title": "GET api/v2/admin/wameed-ai/billing/dashboard",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/billing/dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/billing/dashboard with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0363",
    "title": "POST api/v2/admin/wameed-ai/billing/generate-invoices",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/billing/generate-invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/billing/generate-invoices with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0364",
    "title": "POST api/v2/admin/wameed-ai/billing/invoices/{invoiceId}/mark-paid",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/billing/invoices/{invoiceId}/mark-paid. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/billing/invoices/{invoiceId}/mark-paid with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0365",
    "title": "POST api/v2/admin/wameed-ai/billing/invoices/{invoiceId}/record-payment",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/billing/invoices/{invoiceId}/record-payment. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/billing/invoices/{invoiceId}/record-payment with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0366",
    "title": "GET api/v2/admin/wameed-ai/billing/invoices",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/billing/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/billing/invoices with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0367",
    "title": "GET api/v2/admin/wameed-ai/billing/invoices/{invoiceId}",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/billing/invoices/{invoiceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/billing/invoices/{invoiceId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0368",
    "title": "GET api/v2/admin/wameed-ai/billing/settings",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/billing/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/billing/settings with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0369",
    "title": "PUT api/v2/admin/wameed-ai/billing/settings",
    "description": "Smoke test for PUT /api/v2/admin/wameed-ai/billing/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/wameed-ai/billing/settings with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0370",
    "title": "POST api/v2/admin/wameed-ai/billing/stores/{storeId}/toggle-ai",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/billing/stores/{storeId}/toggle-ai. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/billing/stores/{storeId}/toggle-ai with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0371",
    "title": "GET api/v2/admin/wameed-ai/billing/stores",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/billing/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/billing/stores with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0372",
    "title": "GET api/v2/admin/wameed-ai/billing/stores/{storeId}",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/billing/stores/{storeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/billing/stores/{storeId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0373",
    "title": "PUT api/v2/admin/wameed-ai/billing/stores/{storeId}",
    "description": "Smoke test for PUT /api/v2/admin/wameed-ai/billing/stores/{storeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/wameed-ai/billing/stores/{storeId} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0374",
    "title": "GET api/v2/admin/wameed-ai/features",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/features. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/features with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0375",
    "title": "PATCH api/v2/admin/wameed-ai/features/{featureId}/toggle",
    "description": "Smoke test for PATCH /api/v2/admin/wameed-ai/features/{featureId}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/admin/wameed-ai/features/{featureId}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0376",
    "title": "POST api/v2/admin/wameed-ai/llm-models",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/llm-models. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/llm-models with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0377",
    "title": "GET api/v2/admin/wameed-ai/llm-models",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/llm-models. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/llm-models with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0378",
    "title": "PUT api/v2/admin/wameed-ai/llm-models/{id}",
    "description": "Smoke test for PUT /api/v2/admin/wameed-ai/llm-models/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/wameed-ai/llm-models/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0379",
    "title": "PATCH api/v2/admin/wameed-ai/llm-models/{id}/toggle",
    "description": "Smoke test for PATCH /api/v2/admin/wameed-ai/llm-models/{id}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/admin/wameed-ai/llm-models/{id}/toggle with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0380",
    "title": "DELETE api/v2/admin/wameed-ai/llm-models/{id}",
    "description": "Smoke test for DELETE /api/v2/admin/wameed-ai/llm-models/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/admin/wameed-ai/llm-models/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0381",
    "title": "GET api/v2/admin/wameed-ai/platform-log-stats",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/platform-log-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/platform-log-stats with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0382",
    "title": "GET api/v2/admin/wameed-ai/platform-logs",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/platform-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/platform-logs with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0383",
    "title": "POST api/v2/admin/wameed-ai/platform-trends",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/platform-trends. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/platform-trends with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0384",
    "title": "GET api/v2/admin/wameed-ai/platform-usage",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/platform-usage. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/platform-usage with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0385",
    "title": "POST api/v2/admin/wameed-ai/providers",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/providers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/providers with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0386",
    "title": "GET api/v2/admin/wameed-ai/providers",
    "description": "Smoke test for GET /api/v2/admin/wameed-ai/providers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/admin/wameed-ai/providers with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401."
  },
  {
    "id": "TC0387",
    "title": "PUT api/v2/admin/wameed-ai/providers/{id}",
    "description": "Smoke test for PUT /api/v2/admin/wameed-ai/providers/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/admin/wameed-ai/providers/{id} with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0388",
    "title": "POST api/v2/admin/wameed-ai/store-health",
    "description": "Smoke test for POST /api/v2/admin/wameed-ai/store-health. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/admin/wameed-ai/store-health with the provider Bearer token from the seeded owner account. ADMIN ROUTE: Uses admin-api guard. With provider Sanctum token, expect HTTP 401 (not Sanctum). A passing test asserts the call returns 401.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0389",
    "title": "POST api/v2/announcements/{id}/dismiss",
    "description": "Smoke test for POST /api/v2/announcements/{id}/dismiss. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/announcements/{id}/dismiss once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0390",
    "title": "GET api/v2/announcements",
    "description": "Smoke test for GET /api/v2/announcements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/announcements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0391",
    "title": "POST api/v2/appointments",
    "description": "Smoke test for POST /api/v2/appointments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/appointments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0392",
    "title": "POST api/v2/appointments/{id}/cancel",
    "description": "Smoke test for POST /api/v2/appointments/{id}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/appointments/{id}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0393",
    "title": "GET api/v2/appointments",
    "description": "Smoke test for GET /api/v2/appointments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/appointments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0394",
    "title": "PUT api/v2/appointments/{id}",
    "description": "Smoke test for PUT /api/v2/appointments/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/appointments/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0395",
    "title": "POST api/v2/auth/login",
    "description": "Smoke test for POST /api/v2/auth/login. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/login without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0396",
    "title": "POST api/v2/auth/login/pin",
    "description": "Smoke test for POST /api/v2/auth/login/pin. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/login/pin without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0397",
    "title": "POST api/v2/auth/logout",
    "description": "Smoke test for POST /api/v2/auth/logout. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/logout once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0398",
    "title": "POST api/v2/auth/logout-all",
    "description": "Smoke test for POST /api/v2/auth/logout-all. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/logout-all once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0399",
    "title": "GET api/v2/auth/me",
    "description": "Smoke test for GET /api/v2/auth/me. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auth/me once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0400",
    "title": "POST api/v2/auth/otp/send",
    "description": "Smoke test for POST /api/v2/auth/otp/send. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/otp/send without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0401",
    "title": "POST api/v2/auth/otp/verify",
    "description": "Smoke test for POST /api/v2/auth/otp/verify. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/otp/verify without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0402",
    "title": "PUT api/v2/auth/password",
    "description": "Smoke test for PUT /api/v2/auth/password. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/auth/password once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0403",
    "title": "PUT api/v2/auth/pin",
    "description": "Smoke test for PUT /api/v2/auth/pin. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/auth/pin once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0404",
    "title": "PUT api/v2/auth/profile",
    "description": "Smoke test for PUT /api/v2/auth/profile. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/auth/profile once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0405",
    "title": "POST api/v2/auth/refresh",
    "description": "Smoke test for POST /api/v2/auth/refresh. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/refresh once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0406",
    "title": "POST api/v2/auth/register",
    "description": "Smoke test for POST /api/v2/auth/register. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auth/register without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0407",
    "title": "GET api/v2/auto-update/changelog",
    "description": "Smoke test for GET /api/v2/auto-update/changelog. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auto-update/changelog once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0408",
    "title": "POST api/v2/auto-update/check",
    "description": "Smoke test for POST /api/v2/auto-update/check. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auto-update/check once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0409",
    "title": "GET api/v2/auto-update/current-version",
    "description": "Smoke test for GET /api/v2/auto-update/current-version. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auto-update/current-version once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0410",
    "title": "GET api/v2/auto-update/download/{version}",
    "description": "Smoke test for GET /api/v2/auto-update/download/{version}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auto-update/download/{version} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0411",
    "title": "GET api/v2/auto-update/history",
    "description": "Smoke test for GET /api/v2/auto-update/history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auto-update/history once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0412",
    "title": "GET api/v2/auto-update/manifest/{version}",
    "description": "Smoke test for GET /api/v2/auto-update/manifest/{version}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auto-update/manifest/{version} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0413",
    "title": "POST api/v2/auto-update/report-status",
    "description": "Smoke test for POST /api/v2/auto-update/report-status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/auto-update/report-status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0414",
    "title": "GET api/v2/auto-update/rollout-status",
    "description": "Smoke test for GET /api/v2/auto-update/rollout-status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/auto-update/rollout-status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0415",
    "title": "POST api/v2/backup/{backupId}/restore",
    "description": "Smoke test for POST /api/v2/backup/{backupId}/restore. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/backup/{backupId}/restore once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0416",
    "title": "POST api/v2/backup/{backupId}/verify",
    "description": "Smoke test for POST /api/v2/backup/{backupId}/verify. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/backup/{backupId}/verify once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0417",
    "title": "GET api/v2/backup/{backupId}",
    "description": "Smoke test for GET /api/v2/backup/{backupId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/backup/{backupId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0418",
    "title": "DELETE api/v2/backup/{backupId}",
    "description": "Smoke test for DELETE /api/v2/backup/{backupId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/backup/{backupId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0419",
    "title": "POST api/v2/backup/create",
    "description": "Smoke test for POST /api/v2/backup/create. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/backup/create once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0420",
    "title": "POST api/v2/backup/export",
    "description": "Smoke test for POST /api/v2/backup/export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/backup/export once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0421",
    "title": "GET api/v2/backup/list",
    "description": "Smoke test for GET /api/v2/backup/list. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/backup/list once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0422",
    "title": "GET api/v2/backup/provider-status",
    "description": "Smoke test for GET /api/v2/backup/provider-status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/backup/provider-status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0423",
    "title": "GET api/v2/backup/schedule",
    "description": "Smoke test for GET /api/v2/backup/schedule. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/backup/schedule once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0424",
    "title": "PUT api/v2/backup/schedule",
    "description": "Smoke test for PUT /api/v2/backup/schedule. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/backup/schedule once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0425",
    "title": "GET api/v2/backup/storage",
    "description": "Smoke test for GET /api/v2/backup/storage. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/backup/storage once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0426",
    "title": "POST api/v2/cash-events",
    "description": "Smoke test for POST /api/v2/cash-events. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cash-events once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0427",
    "title": "POST api/v2/cash-sessions",
    "description": "Smoke test for POST /api/v2/cash-sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cash-sessions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0428",
    "title": "GET api/v2/cash-sessions",
    "description": "Smoke test for GET /api/v2/cash-sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cash-sessions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0429",
    "title": "GET api/v2/cash-sessions/{id}",
    "description": "Smoke test for GET /api/v2/cash-sessions/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cash-sessions/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0430",
    "title": "PUT api/v2/cash-sessions/{id}/close",
    "description": "Smoke test for PUT /api/v2/cash-sessions/{id}/close. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/cash-sessions/{id}/close once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0431",
    "title": "POST api/v2/cashier-gamification/anomalies/{anomalyId}/review",
    "description": "Smoke test for POST /api/v2/cashier-gamification/anomalies/{anomalyId}/review. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cashier-gamification/anomalies/{anomalyId}/review once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0432",
    "title": "GET api/v2/cashier-gamification/anomalies",
    "description": "Smoke test for GET /api/v2/cashier-gamification/anomalies. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/anomalies once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0433",
    "title": "GET api/v2/cashier-gamification/badge-awards",
    "description": "Smoke test for GET /api/v2/cashier-gamification/badge-awards. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/badge-awards once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0434",
    "title": "POST api/v2/cashier-gamification/badges",
    "description": "Smoke test for POST /api/v2/cashier-gamification/badges. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cashier-gamification/badges once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0435",
    "title": "GET api/v2/cashier-gamification/badges",
    "description": "Smoke test for GET /api/v2/cashier-gamification/badges. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/badges once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0436",
    "title": "PUT api/v2/cashier-gamification/badges/{badgeId}",
    "description": "Smoke test for PUT /api/v2/cashier-gamification/badges/{badgeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/cashier-gamification/badges/{badgeId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0437",
    "title": "DELETE api/v2/cashier-gamification/badges/{badgeId}",
    "description": "Smoke test for DELETE /api/v2/cashier-gamification/badges/{badgeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/cashier-gamification/badges/{badgeId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0438",
    "title": "POST api/v2/cashier-gamification/badges/seed",
    "description": "Smoke test for POST /api/v2/cashier-gamification/badges/seed. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cashier-gamification/badges/seed once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0439",
    "title": "GET api/v2/cashier-gamification/cashier/{cashierId}/history",
    "description": "Smoke test for GET /api/v2/cashier-gamification/cashier/{cashierId}/history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/cashier/{cashierId}/history once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0440",
    "title": "POST api/v2/cashier-gamification/generate-snapshot",
    "description": "Smoke test for POST /api/v2/cashier-gamification/generate-snapshot. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cashier-gamification/generate-snapshot once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0441",
    "title": "GET api/v2/cashier-gamification/leaderboard",
    "description": "Smoke test for GET /api/v2/cashier-gamification/leaderboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/leaderboard once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0442",
    "title": "GET api/v2/cashier-gamification/settings",
    "description": "Smoke test for GET /api/v2/cashier-gamification/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0443",
    "title": "PUT api/v2/cashier-gamification/settings",
    "description": "Smoke test for PUT /api/v2/cashier-gamification/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/cashier-gamification/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0444",
    "title": "POST api/v2/cashier-gamification/shift-reports/{reportId}/mark-sent",
    "description": "Smoke test for POST /api/v2/cashier-gamification/shift-reports/{reportId}/mark-sent. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/cashier-gamification/shift-reports/{reportId}/mark-sent once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0445",
    "title": "GET api/v2/cashier-gamification/shift-reports",
    "description": "Smoke test for GET /api/v2/cashier-gamification/shift-reports. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/shift-reports once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0446",
    "title": "GET api/v2/cashier-gamification/shift-reports/{reportId}",
    "description": "Smoke test for GET /api/v2/cashier-gamification/shift-reports/{reportId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cashier-gamification/shift-reports/{reportId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0447",
    "title": "POST api/v2/catalog/categories",
    "description": "Smoke test for POST /api/v2/catalog/categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/catalog/categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0448",
    "title": "GET api/v2/catalog/categories",
    "description": "Smoke test for GET /api/v2/catalog/categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0449",
    "title": "GET api/v2/catalog/categories/{category}",
    "description": "Smoke test for GET /api/v2/catalog/categories/{category}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/categories/{category} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0450",
    "title": "PUT api/v2/catalog/categories/{category}",
    "description": "Smoke test for PUT /api/v2/catalog/categories/{category}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/categories/{category} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0451",
    "title": "DELETE api/v2/catalog/categories/{category}",
    "description": "Smoke test for DELETE /api/v2/catalog/categories/{category}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/catalog/categories/{category} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0452",
    "title": "POST api/v2/catalog/products",
    "description": "Smoke test for POST /api/v2/catalog/products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/catalog/products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0453",
    "title": "POST api/v2/catalog/products/{product}/barcode",
    "description": "Smoke test for POST /api/v2/catalog/products/{product}/barcode. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/catalog/products/{product}/barcode once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0454",
    "title": "POST api/v2/catalog/products/{product}/duplicate",
    "description": "Smoke test for POST /api/v2/catalog/products/{product}/duplicate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/catalog/products/{product}/duplicate once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0455",
    "title": "GET api/v2/catalog/products",
    "description": "Smoke test for GET /api/v2/catalog/products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0456",
    "title": "GET api/v2/catalog/products/{product}",
    "description": "Smoke test for GET /api/v2/catalog/products/{product}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/{product} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0457",
    "title": "GET api/v2/catalog/products/{product}/barcodes",
    "description": "Smoke test for GET /api/v2/catalog/products/{product}/barcodes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/{product}/barcodes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0458",
    "title": "GET api/v2/catalog/products/{product}/modifiers",
    "description": "Smoke test for GET /api/v2/catalog/products/{product}/modifiers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/{product}/modifiers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0459",
    "title": "GET api/v2/catalog/products/{product}/store-prices",
    "description": "Smoke test for GET /api/v2/catalog/products/{product}/store-prices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/{product}/store-prices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0460",
    "title": "GET api/v2/catalog/products/{product}/suppliers",
    "description": "Smoke test for GET /api/v2/catalog/products/{product}/suppliers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/{product}/suppliers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0461",
    "title": "GET api/v2/catalog/products/{product}/variants",
    "description": "Smoke test for GET /api/v2/catalog/products/{product}/variants. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/{product}/variants once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0462",
    "title": "PUT api/v2/catalog/products/{product}",
    "description": "Smoke test for PUT /api/v2/catalog/products/{product}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/products/{product} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0463",
    "title": "PUT api/v2/catalog/products/{product}/modifiers",
    "description": "Smoke test for PUT /api/v2/catalog/products/{product}/modifiers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/products/{product}/modifiers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0464",
    "title": "PUT api/v2/catalog/products/{product}/store-prices",
    "description": "Smoke test for PUT /api/v2/catalog/products/{product}/store-prices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/products/{product}/store-prices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0465",
    "title": "PUT api/v2/catalog/products/{product}/suppliers",
    "description": "Smoke test for PUT /api/v2/catalog/products/{product}/suppliers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/products/{product}/suppliers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0466",
    "title": "PUT api/v2/catalog/products/{product}/variants",
    "description": "Smoke test for PUT /api/v2/catalog/products/{product}/variants. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/products/{product}/variants once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0467",
    "title": "DELETE api/v2/catalog/products/{product}",
    "description": "Smoke test for DELETE /api/v2/catalog/products/{product}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/catalog/products/{product} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0468",
    "title": "POST api/v2/catalog/products/bulk-action",
    "description": "Smoke test for POST /api/v2/catalog/products/bulk-action. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/catalog/products/bulk-action once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0469",
    "title": "GET api/v2/catalog/products/catalog",
    "description": "Smoke test for GET /api/v2/catalog/products/catalog. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/catalog once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0470",
    "title": "GET api/v2/catalog/products/changes",
    "description": "Smoke test for GET /api/v2/catalog/products/changes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/products/changes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0471",
    "title": "POST api/v2/catalog/suppliers",
    "description": "Smoke test for POST /api/v2/catalog/suppliers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/catalog/suppliers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0472",
    "title": "GET api/v2/catalog/suppliers",
    "description": "Smoke test for GET /api/v2/catalog/suppliers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/suppliers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0473",
    "title": "GET api/v2/catalog/suppliers/{supplier}",
    "description": "Smoke test for GET /api/v2/catalog/suppliers/{supplier}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/catalog/suppliers/{supplier} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0474",
    "title": "PUT api/v2/catalog/suppliers/{supplier}",
    "description": "Smoke test for PUT /api/v2/catalog/suppliers/{supplier}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/catalog/suppliers/{supplier} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0475",
    "title": "DELETE api/v2/catalog/suppliers/{supplier}",
    "description": "Smoke test for DELETE /api/v2/catalog/suppliers/{supplier}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/catalog/suppliers/{supplier} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0476",
    "title": "GET api/v2/cfd/config",
    "description": "Smoke test for GET /api/v2/cfd/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/cfd/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0477",
    "title": "PUT api/v2/cfd/config",
    "description": "Smoke test for PUT /api/v2/cfd/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/cfd/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0478",
    "title": "GET api/v2/companion/branches",
    "description": "Smoke test for GET /api/v2/companion/branches. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/branches without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0479",
    "title": "GET api/v2/companion/dashboard",
    "description": "Smoke test for GET /api/v2/companion/dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/dashboard without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0480",
    "title": "POST api/v2/companion/events",
    "description": "Smoke test for POST /api/v2/companion/events. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/companion/events without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0481",
    "title": "GET api/v2/companion/inventory/alerts",
    "description": "Smoke test for GET /api/v2/companion/inventory/alerts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/inventory/alerts without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0482",
    "title": "GET api/v2/companion/orders/active",
    "description": "Smoke test for GET /api/v2/companion/orders/active. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/orders/active without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0483",
    "title": "GET api/v2/companion/preferences",
    "description": "Smoke test for GET /api/v2/companion/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/preferences without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0484",
    "title": "PUT api/v2/companion/preferences",
    "description": "Smoke test for PUT /api/v2/companion/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/companion/preferences without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0485",
    "title": "GET api/v2/companion/quick-actions",
    "description": "Smoke test for GET /api/v2/companion/quick-actions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/quick-actions without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0486",
    "title": "PUT api/v2/companion/quick-actions",
    "description": "Smoke test for PUT /api/v2/companion/quick-actions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/companion/quick-actions without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0487",
    "title": "GET api/v2/companion/quick-stats",
    "description": "Smoke test for GET /api/v2/companion/quick-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/quick-stats without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0488",
    "title": "GET api/v2/companion/sales/summary",
    "description": "Smoke test for GET /api/v2/companion/sales/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/sales/summary without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0489",
    "title": "POST api/v2/companion/sessions",
    "description": "Smoke test for POST /api/v2/companion/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/companion/sessions without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0490",
    "title": "POST api/v2/companion/sessions/{sessionId}/end",
    "description": "Smoke test for POST /api/v2/companion/sessions/{sessionId}/end. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/companion/sessions/{sessionId}/end without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0491",
    "title": "GET api/v2/companion/sessions",
    "description": "Smoke test for GET /api/v2/companion/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/sessions without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0492",
    "title": "GET api/v2/companion/staff/active",
    "description": "Smoke test for GET /api/v2/companion/staff/active. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/staff/active without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0493",
    "title": "PUT api/v2/companion/store/availability",
    "description": "Smoke test for PUT /api/v2/companion/store/availability. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/companion/store/availability without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0494",
    "title": "GET api/v2/companion/summary",
    "description": "Smoke test for GET /api/v2/companion/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/companion/summary without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0495",
    "title": "GET api/v2/config/age-restrictions",
    "description": "Smoke test for GET /api/v2/config/age-restrictions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/age-restrictions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0496",
    "title": "GET api/v2/config/feature-flags",
    "description": "Smoke test for GET /api/v2/config/feature-flags. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/feature-flags once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0497",
    "title": "GET api/v2/config/hardware-catalog",
    "description": "Smoke test for GET /api/v2/config/hardware-catalog. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/hardware-catalog once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0498",
    "title": "GET api/v2/config/locales",
    "description": "Smoke test for GET /api/v2/config/locales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/locales once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0499",
    "title": "GET api/v2/config/maintenance",
    "description": "Smoke test for GET /api/v2/config/maintenance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/maintenance without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0500",
    "title": "GET api/v2/config/payment-methods",
    "description": "Smoke test for GET /api/v2/config/payment-methods. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/payment-methods once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0501",
    "title": "GET api/v2/config/security-policies",
    "description": "Smoke test for GET /api/v2/config/security-policies. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/security-policies once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0502",
    "title": "GET api/v2/config/tax",
    "description": "Smoke test for GET /api/v2/config/tax. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/tax once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0503",
    "title": "GET api/v2/config/translations/{locale}",
    "description": "Smoke test for GET /api/v2/config/translations/{locale}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/translations/{locale} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0504",
    "title": "GET api/v2/config/translations/version",
    "description": "Smoke test for GET /api/v2/config/translations/version. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/config/translations/version once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0505",
    "title": "GET api/v2/core/business-types",
    "description": "Smoke test for GET /api/v2/core/business-types. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/business-types once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0506",
    "title": "POST api/v2/core/onboarding/checklist",
    "description": "Smoke test for POST /api/v2/core/onboarding/checklist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/onboarding/checklist once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0507",
    "title": "POST api/v2/core/onboarding/complete-step",
    "description": "Smoke test for POST /api/v2/core/onboarding/complete-step. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/onboarding/complete-step once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0508",
    "title": "POST api/v2/core/onboarding/dismiss-checklist",
    "description": "Smoke test for POST /api/v2/core/onboarding/dismiss-checklist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/onboarding/dismiss-checklist once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0509",
    "title": "GET api/v2/core/onboarding/progress",
    "description": "Smoke test for GET /api/v2/core/onboarding/progress. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/onboarding/progress once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0510",
    "title": "POST api/v2/core/onboarding/reset",
    "description": "Smoke test for POST /api/v2/core/onboarding/reset. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/onboarding/reset once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0511",
    "title": "POST api/v2/core/onboarding/skip",
    "description": "Smoke test for POST /api/v2/core/onboarding/skip. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/onboarding/skip once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0512",
    "title": "GET api/v2/core/onboarding/steps",
    "description": "Smoke test for GET /api/v2/core/onboarding/steps. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/onboarding/steps once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0513",
    "title": "POST api/v2/core/stores",
    "description": "Smoke test for POST /api/v2/core/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/stores once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0514",
    "title": "POST api/v2/core/stores/{id}/business-type",
    "description": "Smoke test for POST /api/v2/core/stores/{id}/business-type. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/stores/{id}/business-type once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0515",
    "title": "POST api/v2/core/stores/{id}/copy-settings",
    "description": "Smoke test for POST /api/v2/core/stores/{id}/copy-settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/stores/{id}/copy-settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0516",
    "title": "POST api/v2/core/stores/{id}/copy-working-hours",
    "description": "Smoke test for POST /api/v2/core/stores/{id}/copy-working-hours. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/stores/{id}/copy-working-hours once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0517",
    "title": "POST api/v2/core/stores/{id}/toggle-active",
    "description": "Smoke test for POST /api/v2/core/stores/{id}/toggle-active. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/core/stores/{id}/toggle-active once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0518",
    "title": "GET api/v2/core/stores",
    "description": "Smoke test for GET /api/v2/core/stores. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0519",
    "title": "GET api/v2/core/stores/{id}",
    "description": "Smoke test for GET /api/v2/core/stores/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0520",
    "title": "GET api/v2/core/stores/{id}/settings",
    "description": "Smoke test for GET /api/v2/core/stores/{id}/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores/{id}/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0521",
    "title": "GET api/v2/core/stores/{id}/working-hours",
    "description": "Smoke test for GET /api/v2/core/stores/{id}/working-hours. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores/{id}/working-hours once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0522",
    "title": "PUT api/v2/core/stores/{id}",
    "description": "Smoke test for PUT /api/v2/core/stores/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/core/stores/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0523",
    "title": "PUT api/v2/core/stores/{id}/settings",
    "description": "Smoke test for PUT /api/v2/core/stores/{id}/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/core/stores/{id}/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0524",
    "title": "PUT api/v2/core/stores/{id}/working-hours",
    "description": "Smoke test for PUT /api/v2/core/stores/{id}/working-hours. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/core/stores/{id}/working-hours once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0525",
    "title": "DELETE api/v2/core/stores/{id}",
    "description": "Smoke test for DELETE /api/v2/core/stores/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/core/stores/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0526",
    "title": "GET api/v2/core/stores/managers",
    "description": "Smoke test for GET /api/v2/core/stores/managers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores/managers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0527",
    "title": "GET api/v2/core/stores/mine",
    "description": "Smoke test for GET /api/v2/core/stores/mine. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores/mine once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0528",
    "title": "PUT api/v2/core/stores/sort-order",
    "description": "Smoke test for PUT /api/v2/core/stores/sort-order. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/core/stores/sort-order once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0529",
    "title": "GET api/v2/core/stores/stats",
    "description": "Smoke test for GET /api/v2/core/stores/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/core/stores/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0530",
    "title": "POST api/v2/coupons/redeem",
    "description": "Smoke test for POST /api/v2/coupons/redeem. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/coupons/redeem once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0531",
    "title": "POST api/v2/coupons/validate",
    "description": "Smoke test for POST /api/v2/coupons/validate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/coupons/validate once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0532",
    "title": "POST api/v2/customers",
    "description": "Smoke test for POST /api/v2/customers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/customers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0533",
    "title": "POST api/v2/customers/{customer}/loyalty/adjust",
    "description": "Smoke test for POST /api/v2/customers/{customer}/loyalty/adjust. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/customers/{customer}/loyalty/adjust once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0534",
    "title": "POST api/v2/customers/{customer}/store-credit/top-up",
    "description": "Smoke test for POST /api/v2/customers/{customer}/store-credit/top-up. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/customers/{customer}/store-credit/top-up once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0535",
    "title": "GET api/v2/customers",
    "description": "Smoke test for GET /api/v2/customers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0536",
    "title": "GET api/v2/customers/{customer}",
    "description": "Smoke test for GET /api/v2/customers/{customer}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customers/{customer} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0537",
    "title": "GET api/v2/customers/{customer}/loyalty",
    "description": "Smoke test for GET /api/v2/customers/{customer}/loyalty. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customers/{customer}/loyalty once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0538",
    "title": "GET api/v2/customers/{customer}/store-credit",
    "description": "Smoke test for GET /api/v2/customers/{customer}/store-credit. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customers/{customer}/store-credit once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0539",
    "title": "PUT api/v2/customers/{customer}",
    "description": "Smoke test for PUT /api/v2/customers/{customer}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/customers/{customer} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0540",
    "title": "DELETE api/v2/customers/{customer}",
    "description": "Smoke test for DELETE /api/v2/customers/{customer}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/customers/{customer} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0541",
    "title": "POST api/v2/customers/groups",
    "description": "Smoke test for POST /api/v2/customers/groups. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/customers/groups once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0542",
    "title": "PUT api/v2/customers/groups/{group}",
    "description": "Smoke test for PUT /api/v2/customers/groups/{group}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/customers/groups/{group} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0543",
    "title": "DELETE api/v2/customers/groups/{group}",
    "description": "Smoke test for DELETE /api/v2/customers/groups/{group}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/customers/groups/{group} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0544",
    "title": "GET api/v2/customers/groups/list",
    "description": "Smoke test for GET /api/v2/customers/groups/list. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customers/groups/list once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0545",
    "title": "GET api/v2/customers/loyalty/config",
    "description": "Smoke test for GET /api/v2/customers/loyalty/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customers/loyalty/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0546",
    "title": "PUT api/v2/customers/loyalty/config",
    "description": "Smoke test for PUT /api/v2/customers/loyalty/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/customers/loyalty/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0547",
    "title": "GET api/v2/customization/export",
    "description": "Smoke test for GET /api/v2/customization/export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customization/export once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0548",
    "title": "GET api/v2/customization/quick-access",
    "description": "Smoke test for GET /api/v2/customization/quick-access. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customization/quick-access once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0549",
    "title": "PUT api/v2/customization/quick-access",
    "description": "Smoke test for PUT /api/v2/customization/quick-access. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/customization/quick-access once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0550",
    "title": "DELETE api/v2/customization/quick-access",
    "description": "Smoke test for DELETE /api/v2/customization/quick-access. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/customization/quick-access once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0551",
    "title": "GET api/v2/customization/receipt",
    "description": "Smoke test for GET /api/v2/customization/receipt. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customization/receipt once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0552",
    "title": "PUT api/v2/customization/receipt",
    "description": "Smoke test for PUT /api/v2/customization/receipt. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/customization/receipt once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0553",
    "title": "DELETE api/v2/customization/receipt",
    "description": "Smoke test for DELETE /api/v2/customization/receipt. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/customization/receipt once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0554",
    "title": "GET api/v2/customization/settings",
    "description": "Smoke test for GET /api/v2/customization/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/customization/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0555",
    "title": "PUT api/v2/customization/settings",
    "description": "Smoke test for PUT /api/v2/customization/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/customization/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0556",
    "title": "DELETE api/v2/customization/settings",
    "description": "Smoke test for DELETE /api/v2/customization/settings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/customization/settings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0557",
    "title": "POST api/v2/debits",
    "description": "Smoke test for POST /api/v2/debits. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/debits once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0558",
    "title": "POST api/v2/debits/{debit}/allocate",
    "description": "Smoke test for POST /api/v2/debits/{debit}/allocate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/debits/{debit}/allocate once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0559",
    "title": "POST api/v2/debits/{debit}/reverse",
    "description": "Smoke test for POST /api/v2/debits/{debit}/reverse. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/debits/{debit}/reverse once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0560",
    "title": "GET api/v2/debits",
    "description": "Smoke test for GET /api/v2/debits. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/debits once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0561",
    "title": "GET api/v2/debits/{debit}",
    "description": "Smoke test for GET /api/v2/debits/{debit}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/debits/{debit} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0562",
    "title": "GET api/v2/debits/{debit}/allocations",
    "description": "Smoke test for GET /api/v2/debits/{debit}/allocations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/debits/{debit}/allocations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0563",
    "title": "PUT api/v2/debits/{debit}",
    "description": "Smoke test for PUT /api/v2/debits/{debit}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/debits/{debit} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0564",
    "title": "DELETE api/v2/debits/{debit}",
    "description": "Smoke test for DELETE /api/v2/debits/{debit}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/debits/{debit} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0565",
    "title": "GET api/v2/debits/customer/{customerId}",
    "description": "Smoke test for GET /api/v2/debits/customer/{customerId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/debits/customer/{customerId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0566",
    "title": "GET api/v2/debits/customer/{customerId}/balance",
    "description": "Smoke test for GET /api/v2/debits/customer/{customerId}/balance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/debits/customer/{customerId}/balance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0567",
    "title": "GET api/v2/debits/summary",
    "description": "Smoke test for GET /api/v2/debits/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/debits/summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0568",
    "title": "POST api/v2/delivery/configs",
    "description": "Smoke test for POST /api/v2/delivery/configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/delivery/configs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0569",
    "title": "POST api/v2/delivery/configs/{id}/test-connection",
    "description": "Smoke test for POST /api/v2/delivery/configs/{id}/test-connection. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/delivery/configs/{id}/test-connection once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0570",
    "title": "GET api/v2/delivery/configs",
    "description": "Smoke test for GET /api/v2/delivery/configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/configs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0571",
    "title": "GET api/v2/delivery/configs/{id}",
    "description": "Smoke test for GET /api/v2/delivery/configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/configs/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0572",
    "title": "PUT api/v2/delivery/configs/{id}/toggle",
    "description": "Smoke test for PUT /api/v2/delivery/configs/{id}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/delivery/configs/{id}/toggle once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0573",
    "title": "DELETE api/v2/delivery/configs/{id}",
    "description": "Smoke test for DELETE /api/v2/delivery/configs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/delivery/configs/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0574",
    "title": "POST api/v2/delivery/menu-sync",
    "description": "Smoke test for POST /api/v2/delivery/menu-sync. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/delivery/menu-sync once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0575",
    "title": "GET api/v2/delivery/orders",
    "description": "Smoke test for GET /api/v2/delivery/orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0576",
    "title": "GET api/v2/delivery/orders/{id}",
    "description": "Smoke test for GET /api/v2/delivery/orders/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/orders/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0577",
    "title": "PUT api/v2/delivery/orders/{id}/status",
    "description": "Smoke test for PUT /api/v2/delivery/orders/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/delivery/orders/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0578",
    "title": "GET api/v2/delivery/orders/active",
    "description": "Smoke test for GET /api/v2/delivery/orders/active. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/orders/active once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0579",
    "title": "GET api/v2/delivery/platforms",
    "description": "Smoke test for GET /api/v2/delivery/platforms. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/platforms once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0580",
    "title": "GET api/v2/delivery/stats",
    "description": "Smoke test for GET /api/v2/delivery/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0581",
    "title": "GET api/v2/delivery/status-push-logs",
    "description": "Smoke test for GET /api/v2/delivery/status-push-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/status-push-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0582",
    "title": "GET api/v2/delivery/sync-logs",
    "description": "Smoke test for GET /api/v2/delivery/sync-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/sync-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0583",
    "title": "POST api/v2/delivery/webhook/{platform}/{storeId}",
    "description": "Smoke test for POST /api/v2/delivery/webhook/{platform}/{storeId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/delivery/webhook/{platform}/{storeId} without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0584",
    "title": "GET api/v2/delivery/webhook-logs",
    "description": "Smoke test for GET /api/v2/delivery/webhook-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/delivery/webhook-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0585",
    "title": "POST api/v2/expenses",
    "description": "Smoke test for POST /api/v2/expenses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/expenses once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0586",
    "title": "GET api/v2/expenses",
    "description": "Smoke test for GET /api/v2/expenses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/expenses once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0587",
    "title": "GET api/v2/finance/daily-summary",
    "description": "Smoke test for GET /api/v2/finance/daily-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/finance/daily-summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0588",
    "title": "GET api/v2/finance/reconciliation",
    "description": "Smoke test for GET /api/v2/finance/reconciliation. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/finance/reconciliation once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0589",
    "title": "GET api/v2/gamification/badges",
    "description": "Smoke test for GET /api/v2/gamification/badges. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gamification/badges once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0590",
    "title": "GET api/v2/gamification/challenges",
    "description": "Smoke test for GET /api/v2/gamification/challenges. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gamification/challenges once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0591",
    "title": "GET api/v2/gamification/customer/{customerId}/badges",
    "description": "Smoke test for GET /api/v2/gamification/customer/{customerId}/badges. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gamification/customer/{customerId}/badges once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0592",
    "title": "GET api/v2/gamification/customer/{customerId}/progress",
    "description": "Smoke test for GET /api/v2/gamification/customer/{customerId}/progress. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gamification/customer/{customerId}/progress once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0593",
    "title": "GET api/v2/gamification/tiers",
    "description": "Smoke test for GET /api/v2/gamification/tiers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gamification/tiers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0594",
    "title": "POST api/v2/gift-cards",
    "description": "Smoke test for POST /api/v2/gift-cards. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/gift-cards once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0595",
    "title": "POST api/v2/gift-cards/{code}/redeem",
    "description": "Smoke test for POST /api/v2/gift-cards/{code}/redeem. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/gift-cards/{code}/redeem once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0596",
    "title": "GET api/v2/gift-cards/{code}/balance",
    "description": "Smoke test for GET /api/v2/gift-cards/{code}/balance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gift-cards/{code}/balance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0597",
    "title": "POST api/v2/gift-registry",
    "description": "Smoke test for POST /api/v2/gift-registry. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/gift-registry once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0598",
    "title": "POST api/v2/gift-registry/{registryId}/items",
    "description": "Smoke test for POST /api/v2/gift-registry/{registryId}/items. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/gift-registry/{registryId}/items once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0599",
    "title": "GET api/v2/gift-registry",
    "description": "Smoke test for GET /api/v2/gift-registry. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gift-registry once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0600",
    "title": "GET api/v2/gift-registry/{registryId}/items",
    "description": "Smoke test for GET /api/v2/gift-registry/{registryId}/items. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gift-registry/{registryId}/items once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0601",
    "title": "GET api/v2/gift-registry/share/{code}",
    "description": "Smoke test for GET /api/v2/gift-registry/share/{code}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/gift-registry/share/{code} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0602",
    "title": "POST api/v2/hardware/config",
    "description": "Smoke test for POST /api/v2/hardware/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/hardware/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0603",
    "title": "GET api/v2/hardware/config",
    "description": "Smoke test for GET /api/v2/hardware/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/hardware/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0604",
    "title": "DELETE api/v2/hardware/config/{id}",
    "description": "Smoke test for DELETE /api/v2/hardware/config/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/hardware/config/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0605",
    "title": "POST api/v2/hardware/event-log",
    "description": "Smoke test for POST /api/v2/hardware/event-log. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/hardware/event-log once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0606",
    "title": "GET api/v2/hardware/event-logs",
    "description": "Smoke test for GET /api/v2/hardware/event-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/hardware/event-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0607",
    "title": "GET api/v2/hardware/supported-models",
    "description": "Smoke test for GET /api/v2/hardware/supported-models. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/hardware/supported-models once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0608",
    "title": "POST api/v2/hardware/test",
    "description": "Smoke test for POST /api/v2/hardware/test. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/hardware/test once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0609",
    "title": "POST api/v2/industry/bakery/cake-orders",
    "description": "Smoke test for POST /api/v2/industry/bakery/cake-orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/bakery/cake-orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0610",
    "title": "GET api/v2/industry/bakery/cake-orders",
    "description": "Smoke test for GET /api/v2/industry/bakery/cake-orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/bakery/cake-orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0611",
    "title": "PUT api/v2/industry/bakery/cake-orders/{id}",
    "description": "Smoke test for PUT /api/v2/industry/bakery/cake-orders/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/bakery/cake-orders/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0612",
    "title": "PATCH api/v2/industry/bakery/cake-orders/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/bakery/cake-orders/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/bakery/cake-orders/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0613",
    "title": "POST api/v2/industry/bakery/production-schedules",
    "description": "Smoke test for POST /api/v2/industry/bakery/production-schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/bakery/production-schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0614",
    "title": "GET api/v2/industry/bakery/production-schedules",
    "description": "Smoke test for GET /api/v2/industry/bakery/production-schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/bakery/production-schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0615",
    "title": "PUT api/v2/industry/bakery/production-schedules/{id}",
    "description": "Smoke test for PUT /api/v2/industry/bakery/production-schedules/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/bakery/production-schedules/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0616",
    "title": "PATCH api/v2/industry/bakery/production-schedules/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/bakery/production-schedules/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/bakery/production-schedules/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0617",
    "title": "POST api/v2/industry/bakery/recipes",
    "description": "Smoke test for POST /api/v2/industry/bakery/recipes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/bakery/recipes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0618",
    "title": "GET api/v2/industry/bakery/recipes",
    "description": "Smoke test for GET /api/v2/industry/bakery/recipes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/bakery/recipes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0619",
    "title": "PUT api/v2/industry/bakery/recipes/{id}",
    "description": "Smoke test for PUT /api/v2/industry/bakery/recipes/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/bakery/recipes/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0620",
    "title": "DELETE api/v2/industry/bakery/recipes/{id}",
    "description": "Smoke test for DELETE /api/v2/industry/bakery/recipes/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/industry/bakery/recipes/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0621",
    "title": "POST api/v2/industry/electronics/imei-records",
    "description": "Smoke test for POST /api/v2/industry/electronics/imei-records. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/electronics/imei-records once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0622",
    "title": "GET api/v2/industry/electronics/imei-records",
    "description": "Smoke test for GET /api/v2/industry/electronics/imei-records. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/electronics/imei-records once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0623",
    "title": "PUT api/v2/industry/electronics/imei-records/{id}",
    "description": "Smoke test for PUT /api/v2/industry/electronics/imei-records/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/electronics/imei-records/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0624",
    "title": "POST api/v2/industry/electronics/repair-jobs",
    "description": "Smoke test for POST /api/v2/industry/electronics/repair-jobs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/electronics/repair-jobs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0625",
    "title": "GET api/v2/industry/electronics/repair-jobs",
    "description": "Smoke test for GET /api/v2/industry/electronics/repair-jobs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/electronics/repair-jobs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0626",
    "title": "PUT api/v2/industry/electronics/repair-jobs/{id}",
    "description": "Smoke test for PUT /api/v2/industry/electronics/repair-jobs/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/electronics/repair-jobs/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0627",
    "title": "PATCH api/v2/industry/electronics/repair-jobs/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/electronics/repair-jobs/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/electronics/repair-jobs/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0628",
    "title": "POST api/v2/industry/electronics/trade-ins",
    "description": "Smoke test for POST /api/v2/industry/electronics/trade-ins. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/electronics/trade-ins once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0629",
    "title": "GET api/v2/industry/electronics/trade-ins",
    "description": "Smoke test for GET /api/v2/industry/electronics/trade-ins. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/electronics/trade-ins once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0630",
    "title": "POST api/v2/industry/florist/arrangements",
    "description": "Smoke test for POST /api/v2/industry/florist/arrangements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/florist/arrangements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0631",
    "title": "GET api/v2/industry/florist/arrangements",
    "description": "Smoke test for GET /api/v2/industry/florist/arrangements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/florist/arrangements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0632",
    "title": "PUT api/v2/industry/florist/arrangements/{id}",
    "description": "Smoke test for PUT /api/v2/industry/florist/arrangements/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/florist/arrangements/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0633",
    "title": "DELETE api/v2/industry/florist/arrangements/{id}",
    "description": "Smoke test for DELETE /api/v2/industry/florist/arrangements/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/industry/florist/arrangements/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0634",
    "title": "POST api/v2/industry/florist/freshness-logs",
    "description": "Smoke test for POST /api/v2/industry/florist/freshness-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/florist/freshness-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0635",
    "title": "GET api/v2/industry/florist/freshness-logs",
    "description": "Smoke test for GET /api/v2/industry/florist/freshness-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/florist/freshness-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0636",
    "title": "PATCH api/v2/industry/florist/freshness-logs/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/florist/freshness-logs/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/florist/freshness-logs/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0637",
    "title": "POST api/v2/industry/florist/subscriptions",
    "description": "Smoke test for POST /api/v2/industry/florist/subscriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/florist/subscriptions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0638",
    "title": "GET api/v2/industry/florist/subscriptions",
    "description": "Smoke test for GET /api/v2/industry/florist/subscriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/florist/subscriptions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0639",
    "title": "PUT api/v2/industry/florist/subscriptions/{id}",
    "description": "Smoke test for PUT /api/v2/industry/florist/subscriptions/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/florist/subscriptions/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0640",
    "title": "PATCH api/v2/industry/florist/subscriptions/{id}/toggle",
    "description": "Smoke test for PATCH /api/v2/industry/florist/subscriptions/{id}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/florist/subscriptions/{id}/toggle once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0641",
    "title": "POST api/v2/industry/jewelry/buybacks",
    "description": "Smoke test for POST /api/v2/industry/jewelry/buybacks. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/jewelry/buybacks once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0642",
    "title": "GET api/v2/industry/jewelry/buybacks",
    "description": "Smoke test for GET /api/v2/industry/jewelry/buybacks. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/jewelry/buybacks once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0643",
    "title": "POST api/v2/industry/jewelry/metal-rates",
    "description": "Smoke test for POST /api/v2/industry/jewelry/metal-rates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/jewelry/metal-rates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0644",
    "title": "GET api/v2/industry/jewelry/metal-rates",
    "description": "Smoke test for GET /api/v2/industry/jewelry/metal-rates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/jewelry/metal-rates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0645",
    "title": "POST api/v2/industry/jewelry/product-details",
    "description": "Smoke test for POST /api/v2/industry/jewelry/product-details. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/jewelry/product-details once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0646",
    "title": "GET api/v2/industry/jewelry/product-details",
    "description": "Smoke test for GET /api/v2/industry/jewelry/product-details. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/jewelry/product-details once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0647",
    "title": "PUT api/v2/industry/jewelry/product-details/{id}",
    "description": "Smoke test for PUT /api/v2/industry/jewelry/product-details/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/jewelry/product-details/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0648",
    "title": "POST api/v2/industry/pharmacy/drug-schedules",
    "description": "Smoke test for POST /api/v2/industry/pharmacy/drug-schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/pharmacy/drug-schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0649",
    "title": "GET api/v2/industry/pharmacy/drug-schedules",
    "description": "Smoke test for GET /api/v2/industry/pharmacy/drug-schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/pharmacy/drug-schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0650",
    "title": "PUT api/v2/industry/pharmacy/drug-schedules/{id}",
    "description": "Smoke test for PUT /api/v2/industry/pharmacy/drug-schedules/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/pharmacy/drug-schedules/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0651",
    "title": "POST api/v2/industry/pharmacy/prescriptions",
    "description": "Smoke test for POST /api/v2/industry/pharmacy/prescriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/pharmacy/prescriptions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0652",
    "title": "GET api/v2/industry/pharmacy/prescriptions",
    "description": "Smoke test for GET /api/v2/industry/pharmacy/prescriptions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/pharmacy/prescriptions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0653",
    "title": "PUT api/v2/industry/pharmacy/prescriptions/{id}",
    "description": "Smoke test for PUT /api/v2/industry/pharmacy/prescriptions/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/pharmacy/prescriptions/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0654",
    "title": "POST api/v2/industry/restaurant/kitchen-tickets",
    "description": "Smoke test for POST /api/v2/industry/restaurant/kitchen-tickets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/restaurant/kitchen-tickets once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0655",
    "title": "GET api/v2/industry/restaurant/kitchen-tickets",
    "description": "Smoke test for GET /api/v2/industry/restaurant/kitchen-tickets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/restaurant/kitchen-tickets once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0656",
    "title": "PATCH api/v2/industry/restaurant/kitchen-tickets/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/restaurant/kitchen-tickets/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/restaurant/kitchen-tickets/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0657",
    "title": "POST api/v2/industry/restaurant/reservations",
    "description": "Smoke test for POST /api/v2/industry/restaurant/reservations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/restaurant/reservations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0658",
    "title": "GET api/v2/industry/restaurant/reservations",
    "description": "Smoke test for GET /api/v2/industry/restaurant/reservations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/restaurant/reservations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0659",
    "title": "PUT api/v2/industry/restaurant/reservations/{id}",
    "description": "Smoke test for PUT /api/v2/industry/restaurant/reservations/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/restaurant/reservations/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0660",
    "title": "PATCH api/v2/industry/restaurant/reservations/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/restaurant/reservations/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/restaurant/reservations/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0661",
    "title": "POST api/v2/industry/restaurant/tables",
    "description": "Smoke test for POST /api/v2/industry/restaurant/tables. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/restaurant/tables once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0662",
    "title": "GET api/v2/industry/restaurant/tables",
    "description": "Smoke test for GET /api/v2/industry/restaurant/tables. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/restaurant/tables once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0663",
    "title": "PUT api/v2/industry/restaurant/tables/{id}",
    "description": "Smoke test for PUT /api/v2/industry/restaurant/tables/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/industry/restaurant/tables/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0664",
    "title": "PATCH api/v2/industry/restaurant/tables/{id}/status",
    "description": "Smoke test for PATCH /api/v2/industry/restaurant/tables/{id}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/restaurant/tables/{id}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0665",
    "title": "POST api/v2/industry/restaurant/tabs",
    "description": "Smoke test for POST /api/v2/industry/restaurant/tabs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/industry/restaurant/tabs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0666",
    "title": "GET api/v2/industry/restaurant/tabs",
    "description": "Smoke test for GET /api/v2/industry/restaurant/tabs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/industry/restaurant/tabs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0667",
    "title": "PATCH api/v2/industry/restaurant/tabs/{id}/close",
    "description": "Smoke test for PATCH /api/v2/industry/restaurant/tabs/{id}/close. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/industry/restaurant/tabs/{id}/close once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0668",
    "title": "POST api/v2/installments/{id}/cancel",
    "description": "Smoke test for POST /api/v2/installments/{id}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/{id}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0669",
    "title": "POST api/v2/installments/{id}/confirm",
    "description": "Smoke test for POST /api/v2/installments/{id}/confirm. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/{id}/confirm once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0670",
    "title": "POST api/v2/installments/{id}/fail",
    "description": "Smoke test for POST /api/v2/installments/{id}/fail. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/{id}/fail once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0671",
    "title": "GET api/v2/installments",
    "description": "Smoke test for GET /api/v2/installments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0672",
    "title": "GET api/v2/installments/{id}",
    "description": "Smoke test for GET /api/v2/installments/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0673",
    "title": "POST api/v2/installments/callback/{provider}/cancel",
    "description": "Smoke test for POST /api/v2/installments/callback/{provider}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/callback/{provider}/cancel without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0674",
    "title": "POST api/v2/installments/callback/{provider}/failure",
    "description": "Smoke test for POST /api/v2/installments/callback/{provider}/failure. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/callback/{provider}/failure without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0675",
    "title": "POST api/v2/installments/callback/{provider}/success",
    "description": "Smoke test for POST /api/v2/installments/callback/{provider}/success. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/callback/{provider}/success without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0676",
    "title": "GET api/v2/installments/callback/{provider}/cancel",
    "description": "Smoke test for GET /api/v2/installments/callback/{provider}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/callback/{provider}/cancel without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0677",
    "title": "GET api/v2/installments/callback/{provider}/failure",
    "description": "Smoke test for GET /api/v2/installments/callback/{provider}/failure. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/callback/{provider}/failure without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0678",
    "title": "GET api/v2/installments/callback/{provider}/success",
    "description": "Smoke test for GET /api/v2/installments/callback/{provider}/success. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/callback/{provider}/success without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0679",
    "title": "POST api/v2/installments/checkout",
    "description": "Smoke test for POST /api/v2/installments/checkout. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/checkout once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0680",
    "title": "POST api/v2/installments/config",
    "description": "Smoke test for POST /api/v2/installments/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0681",
    "title": "POST api/v2/installments/config/{provider}/test",
    "description": "Smoke test for POST /api/v2/installments/config/{provider}/test. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/config/{provider}/test once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0682",
    "title": "POST api/v2/installments/config/{provider}/toggle",
    "description": "Smoke test for POST /api/v2/installments/config/{provider}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/config/{provider}/toggle once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0683",
    "title": "GET api/v2/installments/config",
    "description": "Smoke test for GET /api/v2/installments/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0684",
    "title": "GET api/v2/installments/config/{provider}",
    "description": "Smoke test for GET /api/v2/installments/config/{provider}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/config/{provider} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0685",
    "title": "DELETE api/v2/installments/config/{provider}",
    "description": "Smoke test for DELETE /api/v2/installments/config/{provider}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/installments/config/{provider} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0686",
    "title": "GET api/v2/installments/config/available",
    "description": "Smoke test for GET /api/v2/installments/config/available. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/config/available once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0687",
    "title": "GET api/v2/installments/providers",
    "description": "Smoke test for GET /api/v2/installments/providers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/installments/providers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0688",
    "title": "POST api/v2/installments/tamara-precheck",
    "description": "Smoke test for POST /api/v2/installments/tamara-precheck. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/tamara-precheck once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0689",
    "title": "POST api/v2/installments/webhook/{provider}",
    "description": "Smoke test for POST /api/v2/installments/webhook/{provider}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/installments/webhook/{provider} without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0690",
    "title": "GET api/v2/inventory/expiry-alerts",
    "description": "Smoke test for GET /api/v2/inventory/expiry-alerts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/expiry-alerts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0691",
    "title": "POST api/v2/inventory/goods-receipts",
    "description": "Smoke test for POST /api/v2/inventory/goods-receipts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/goods-receipts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0692",
    "title": "POST api/v2/inventory/goods-receipts/{goodsReceipt}/confirm",
    "description": "Smoke test for POST /api/v2/inventory/goods-receipts/{goodsReceipt}/confirm. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/goods-receipts/{goodsReceipt}/confirm once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0693",
    "title": "GET api/v2/inventory/goods-receipts",
    "description": "Smoke test for GET /api/v2/inventory/goods-receipts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/goods-receipts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0694",
    "title": "GET api/v2/inventory/goods-receipts/{goodsReceipt}",
    "description": "Smoke test for GET /api/v2/inventory/goods-receipts/{goodsReceipt}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/goods-receipts/{goodsReceipt} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0695",
    "title": "GET api/v2/inventory/low-stock",
    "description": "Smoke test for GET /api/v2/inventory/low-stock. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/low-stock once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0696",
    "title": "POST api/v2/inventory/purchase-orders",
    "description": "Smoke test for POST /api/v2/inventory/purchase-orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/purchase-orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0697",
    "title": "POST api/v2/inventory/purchase-orders/{purchaseOrder}/cancel",
    "description": "Smoke test for POST /api/v2/inventory/purchase-orders/{purchaseOrder}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/purchase-orders/{purchaseOrder}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0698",
    "title": "POST api/v2/inventory/purchase-orders/{purchaseOrder}/receive",
    "description": "Smoke test for POST /api/v2/inventory/purchase-orders/{purchaseOrder}/receive. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/purchase-orders/{purchaseOrder}/receive once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0699",
    "title": "POST api/v2/inventory/purchase-orders/{purchaseOrder}/send",
    "description": "Smoke test for POST /api/v2/inventory/purchase-orders/{purchaseOrder}/send. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/purchase-orders/{purchaseOrder}/send once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0700",
    "title": "GET api/v2/inventory/purchase-orders",
    "description": "Smoke test for GET /api/v2/inventory/purchase-orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/purchase-orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0701",
    "title": "GET api/v2/inventory/purchase-orders/{purchaseOrder}",
    "description": "Smoke test for GET /api/v2/inventory/purchase-orders/{purchaseOrder}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/purchase-orders/{purchaseOrder} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0702",
    "title": "POST api/v2/inventory/recipes",
    "description": "Smoke test for POST /api/v2/inventory/recipes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/recipes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0703",
    "title": "GET api/v2/inventory/recipes",
    "description": "Smoke test for GET /api/v2/inventory/recipes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/recipes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0704",
    "title": "GET api/v2/inventory/recipes/{recipe}",
    "description": "Smoke test for GET /api/v2/inventory/recipes/{recipe}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/recipes/{recipe} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0705",
    "title": "PUT api/v2/inventory/recipes/{recipe}",
    "description": "Smoke test for PUT /api/v2/inventory/recipes/{recipe}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/inventory/recipes/{recipe} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0706",
    "title": "DELETE api/v2/inventory/recipes/{recipe}",
    "description": "Smoke test for DELETE /api/v2/inventory/recipes/{recipe}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/inventory/recipes/{recipe} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0707",
    "title": "POST api/v2/inventory/stock-adjustments",
    "description": "Smoke test for POST /api/v2/inventory/stock-adjustments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stock-adjustments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0708",
    "title": "GET api/v2/inventory/stock-adjustments",
    "description": "Smoke test for GET /api/v2/inventory/stock-adjustments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stock-adjustments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0709",
    "title": "GET api/v2/inventory/stock-adjustments/{stockAdjustment}",
    "description": "Smoke test for GET /api/v2/inventory/stock-adjustments/{stockAdjustment}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stock-adjustments/{stockAdjustment} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0710",
    "title": "GET api/v2/inventory/stock-levels",
    "description": "Smoke test for GET /api/v2/inventory/stock-levels. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stock-levels once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0711",
    "title": "PUT api/v2/inventory/stock-levels/{stockLevel}/reorder-point",
    "description": "Smoke test for PUT /api/v2/inventory/stock-levels/{stockLevel}/reorder-point. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/inventory/stock-levels/{stockLevel}/reorder-point once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0712",
    "title": "GET api/v2/inventory/stock-movements",
    "description": "Smoke test for GET /api/v2/inventory/stock-movements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stock-movements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0713",
    "title": "POST api/v2/inventory/stock-transfers",
    "description": "Smoke test for POST /api/v2/inventory/stock-transfers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stock-transfers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0714",
    "title": "POST api/v2/inventory/stock-transfers/{stockTransfer}/approve",
    "description": "Smoke test for POST /api/v2/inventory/stock-transfers/{stockTransfer}/approve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stock-transfers/{stockTransfer}/approve once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0715",
    "title": "POST api/v2/inventory/stock-transfers/{stockTransfer}/cancel",
    "description": "Smoke test for POST /api/v2/inventory/stock-transfers/{stockTransfer}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stock-transfers/{stockTransfer}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0716",
    "title": "POST api/v2/inventory/stock-transfers/{stockTransfer}/receive",
    "description": "Smoke test for POST /api/v2/inventory/stock-transfers/{stockTransfer}/receive. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stock-transfers/{stockTransfer}/receive once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0717",
    "title": "GET api/v2/inventory/stock-transfers",
    "description": "Smoke test for GET /api/v2/inventory/stock-transfers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stock-transfers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0718",
    "title": "GET api/v2/inventory/stock-transfers/{stockTransfer}",
    "description": "Smoke test for GET /api/v2/inventory/stock-transfers/{stockTransfer}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stock-transfers/{stockTransfer} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0719",
    "title": "POST api/v2/inventory/stocktakes",
    "description": "Smoke test for POST /api/v2/inventory/stocktakes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stocktakes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0720",
    "title": "POST api/v2/inventory/stocktakes/{stocktake}/apply",
    "description": "Smoke test for POST /api/v2/inventory/stocktakes/{stocktake}/apply. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stocktakes/{stocktake}/apply once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0721",
    "title": "POST api/v2/inventory/stocktakes/{stocktake}/cancel",
    "description": "Smoke test for POST /api/v2/inventory/stocktakes/{stocktake}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/stocktakes/{stocktake}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0722",
    "title": "GET api/v2/inventory/stocktakes",
    "description": "Smoke test for GET /api/v2/inventory/stocktakes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stocktakes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0723",
    "title": "GET api/v2/inventory/stocktakes/{stocktake}",
    "description": "Smoke test for GET /api/v2/inventory/stocktakes/{stocktake}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/stocktakes/{stocktake} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0724",
    "title": "PUT api/v2/inventory/stocktakes/{stocktake}/counts",
    "description": "Smoke test for PUT /api/v2/inventory/stocktakes/{stocktake}/counts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/inventory/stocktakes/{stocktake}/counts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0725",
    "title": "POST api/v2/inventory/supplier-returns",
    "description": "Smoke test for POST /api/v2/inventory/supplier-returns. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/supplier-returns once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0726",
    "title": "POST api/v2/inventory/supplier-returns/{supplierReturn}/approve",
    "description": "Smoke test for POST /api/v2/inventory/supplier-returns/{supplierReturn}/approve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/supplier-returns/{supplierReturn}/approve once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0727",
    "title": "POST api/v2/inventory/supplier-returns/{supplierReturn}/cancel",
    "description": "Smoke test for POST /api/v2/inventory/supplier-returns/{supplierReturn}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/supplier-returns/{supplierReturn}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0728",
    "title": "POST api/v2/inventory/supplier-returns/{supplierReturn}/complete",
    "description": "Smoke test for POST /api/v2/inventory/supplier-returns/{supplierReturn}/complete. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/supplier-returns/{supplierReturn}/complete once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0729",
    "title": "POST api/v2/inventory/supplier-returns/{supplierReturn}/submit",
    "description": "Smoke test for POST /api/v2/inventory/supplier-returns/{supplierReturn}/submit. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/supplier-returns/{supplierReturn}/submit once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0730",
    "title": "GET api/v2/inventory/supplier-returns",
    "description": "Smoke test for GET /api/v2/inventory/supplier-returns. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/supplier-returns once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0731",
    "title": "GET api/v2/inventory/supplier-returns/{supplierReturn}",
    "description": "Smoke test for GET /api/v2/inventory/supplier-returns/{supplierReturn}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/supplier-returns/{supplierReturn} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0732",
    "title": "PUT api/v2/inventory/supplier-returns/{supplierReturn}",
    "description": "Smoke test for PUT /api/v2/inventory/supplier-returns/{supplierReturn}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/inventory/supplier-returns/{supplierReturn} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0733",
    "title": "DELETE api/v2/inventory/supplier-returns/{supplierReturn}",
    "description": "Smoke test for DELETE /api/v2/inventory/supplier-returns/{supplierReturn}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/inventory/supplier-returns/{supplierReturn} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0734",
    "title": "POST api/v2/inventory/waste-records",
    "description": "Smoke test for POST /api/v2/inventory/waste-records. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/inventory/waste-records once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0735",
    "title": "GET api/v2/inventory/waste-records",
    "description": "Smoke test for GET /api/v2/inventory/waste-records. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/inventory/waste-records once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0736",
    "title": "POST api/v2/labels/print-history",
    "description": "Smoke test for POST /api/v2/labels/print-history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/labels/print-history once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0737",
    "title": "GET api/v2/labels/print-history",
    "description": "Smoke test for GET /api/v2/labels/print-history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/labels/print-history once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0738",
    "title": "POST api/v2/labels/templates",
    "description": "Smoke test for POST /api/v2/labels/templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/labels/templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0739",
    "title": "GET api/v2/labels/templates",
    "description": "Smoke test for GET /api/v2/labels/templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/labels/templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0740",
    "title": "GET api/v2/labels/templates/{template}",
    "description": "Smoke test for GET /api/v2/labels/templates/{template}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/labels/templates/{template} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0741",
    "title": "PUT api/v2/labels/templates/{template}",
    "description": "Smoke test for PUT /api/v2/labels/templates/{template}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/labels/templates/{template} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0742",
    "title": "DELETE api/v2/labels/templates/{template}",
    "description": "Smoke test for DELETE /api/v2/labels/templates/{template}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/labels/templates/{template} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0743",
    "title": "GET api/v2/labels/templates/presets",
    "description": "Smoke test for GET /api/v2/labels/templates/presets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/labels/templates/presets once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0744",
    "title": "GET api/v2/notification-templates",
    "description": "Smoke test for GET /api/v2/notification-templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notification-templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0745",
    "title": "GET api/v2/notification-templates/{id}",
    "description": "Smoke test for GET /api/v2/notification-templates/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notification-templates/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0746",
    "title": "POST api/v2/notification-templates/dispatch",
    "description": "Smoke test for POST /api/v2/notification-templates/dispatch. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/notification-templates/dispatch once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0747",
    "title": "GET api/v2/notification-templates/events",
    "description": "Smoke test for GET /api/v2/notification-templates/events. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notification-templates/events once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0748",
    "title": "GET api/v2/notification-templates/events/{eventKey}",
    "description": "Smoke test for GET /api/v2/notification-templates/events/{eventKey}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notification-templates/events/{eventKey} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0749",
    "title": "POST api/v2/notification-templates/render",
    "description": "Smoke test for POST /api/v2/notification-templates/render. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/notification-templates/render once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0750",
    "title": "POST api/v2/notifications",
    "description": "Smoke test for POST /api/v2/notifications. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/notifications once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0751",
    "title": "GET api/v2/notifications",
    "description": "Smoke test for GET /api/v2/notifications. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0752",
    "title": "PUT api/v2/notifications/{id}/read",
    "description": "Smoke test for PUT /api/v2/notifications/{id}/read. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/notifications/{id}/read once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0753",
    "title": "DELETE api/v2/notifications/{id}",
    "description": "Smoke test for DELETE /api/v2/notifications/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/notifications/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0754",
    "title": "POST api/v2/notifications/batch",
    "description": "Smoke test for POST /api/v2/notifications/batch. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/notifications/batch once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0755",
    "title": "DELETE api/v2/notifications/bulk",
    "description": "Smoke test for DELETE /api/v2/notifications/bulk. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/notifications/bulk once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0756",
    "title": "GET api/v2/notifications/delivery-logs",
    "description": "Smoke test for GET /api/v2/notifications/delivery-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/delivery-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0757",
    "title": "GET api/v2/notifications/delivery-stats",
    "description": "Smoke test for GET /api/v2/notifications/delivery-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/delivery-stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0758",
    "title": "POST api/v2/notifications/fcm-tokens",
    "description": "Smoke test for POST /api/v2/notifications/fcm-tokens. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/notifications/fcm-tokens once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0759",
    "title": "DELETE api/v2/notifications/fcm-tokens",
    "description": "Smoke test for DELETE /api/v2/notifications/fcm-tokens. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/notifications/fcm-tokens once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0760",
    "title": "GET api/v2/notifications/preferences",
    "description": "Smoke test for GET /api/v2/notifications/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0761",
    "title": "PUT api/v2/notifications/preferences",
    "description": "Smoke test for PUT /api/v2/notifications/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/notifications/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0762",
    "title": "PUT api/v2/notifications/read-all",
    "description": "Smoke test for PUT /api/v2/notifications/read-all. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/notifications/read-all once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0763",
    "title": "POST api/v2/notifications/schedules",
    "description": "Smoke test for POST /api/v2/notifications/schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/notifications/schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0764",
    "title": "GET api/v2/notifications/schedules",
    "description": "Smoke test for GET /api/v2/notifications/schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0765",
    "title": "PUT api/v2/notifications/schedules/{id}/cancel",
    "description": "Smoke test for PUT /api/v2/notifications/schedules/{id}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/notifications/schedules/{id}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0766",
    "title": "GET api/v2/notifications/sound-configs",
    "description": "Smoke test for GET /api/v2/notifications/sound-configs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/sound-configs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0767",
    "title": "PUT api/v2/notifications/sound-configs/{eventKey}",
    "description": "Smoke test for PUT /api/v2/notifications/sound-configs/{eventKey}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/notifications/sound-configs/{eventKey} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0768",
    "title": "GET api/v2/notifications/stats",
    "description": "Smoke test for GET /api/v2/notifications/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0769",
    "title": "GET api/v2/notifications/unread-count",
    "description": "Smoke test for GET /api/v2/notifications/unread-count. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/unread-count once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0770",
    "title": "GET api/v2/notifications/unread-count-by-category",
    "description": "Smoke test for GET /api/v2/notifications/unread-count-by-category. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/notifications/unread-count-by-category once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0771",
    "title": "POST api/v2/orders",
    "description": "Smoke test for POST /api/v2/orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0772",
    "title": "POST api/v2/orders/{order}/return",
    "description": "Smoke test for POST /api/v2/orders/{order}/return. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/orders/{order}/return once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0773",
    "title": "POST api/v2/orders/{order}/void",
    "description": "Smoke test for POST /api/v2/orders/{order}/void. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/orders/{order}/void once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0774",
    "title": "GET api/v2/orders",
    "description": "Smoke test for GET /api/v2/orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0775",
    "title": "GET api/v2/orders/{order}",
    "description": "Smoke test for GET /api/v2/orders/{order}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/orders/{order} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0776",
    "title": "PUT api/v2/orders/{order}/status",
    "description": "Smoke test for PUT /api/v2/orders/{order}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/orders/{order}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0777",
    "title": "GET api/v2/orders/returns/{returnId}",
    "description": "Smoke test for GET /api/v2/orders/returns/{returnId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/orders/returns/{returnId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0778",
    "title": "GET api/v2/orders/returns/list",
    "description": "Smoke test for GET /api/v2/orders/returns/list. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/orders/returns/list once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0779",
    "title": "GET api/v2/owner-dashboard/active-cashiers",
    "description": "Smoke test for GET /api/v2/owner-dashboard/active-cashiers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/active-cashiers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0780",
    "title": "GET api/v2/owner-dashboard/branches",
    "description": "Smoke test for GET /api/v2/owner-dashboard/branches. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/branches once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0781",
    "title": "GET api/v2/owner-dashboard/financial-summary",
    "description": "Smoke test for GET /api/v2/owner-dashboard/financial-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/financial-summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0782",
    "title": "GET api/v2/owner-dashboard/hourly-sales",
    "description": "Smoke test for GET /api/v2/owner-dashboard/hourly-sales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/hourly-sales once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0783",
    "title": "GET api/v2/owner-dashboard/low-stock",
    "description": "Smoke test for GET /api/v2/owner-dashboard/low-stock. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/low-stock once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0784",
    "title": "GET api/v2/owner-dashboard/recent-orders",
    "description": "Smoke test for GET /api/v2/owner-dashboard/recent-orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/recent-orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0785",
    "title": "GET api/v2/owner-dashboard/sales-trend",
    "description": "Smoke test for GET /api/v2/owner-dashboard/sales-trend. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/sales-trend once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0786",
    "title": "GET api/v2/owner-dashboard/staff-performance",
    "description": "Smoke test for GET /api/v2/owner-dashboard/staff-performance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/staff-performance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0787",
    "title": "GET api/v2/owner-dashboard/stats",
    "description": "Smoke test for GET /api/v2/owner-dashboard/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0788",
    "title": "GET api/v2/owner-dashboard/top-products",
    "description": "Smoke test for GET /api/v2/owner-dashboard/top-products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/owner-dashboard/top-products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0789",
    "title": "POST api/v2/payments",
    "description": "Smoke test for POST /api/v2/payments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/payments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0790",
    "title": "GET api/v2/payments",
    "description": "Smoke test for GET /api/v2/payments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/payments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0791",
    "title": "GET api/v2/pos/customers",
    "description": "Smoke test for GET /api/v2/pos/customers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/customers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0792",
    "title": "POST api/v2/pos/held-carts",
    "description": "Smoke test for POST /api/v2/pos/held-carts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/held-carts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0793",
    "title": "GET api/v2/pos/held-carts",
    "description": "Smoke test for GET /api/v2/pos/held-carts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/held-carts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0794",
    "title": "PUT api/v2/pos/held-carts/{cart}/recall",
    "description": "Smoke test for PUT /api/v2/pos/held-carts/{cart}/recall. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/pos/held-carts/{cart}/recall once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0795",
    "title": "DELETE api/v2/pos/held-carts/{cart}",
    "description": "Smoke test for DELETE /api/v2/pos/held-carts/{cart}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/pos/held-carts/{cart} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0796",
    "title": "GET api/v2/pos/products",
    "description": "Smoke test for GET /api/v2/pos/products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0797",
    "title": "GET api/v2/pos/registers",
    "description": "Smoke test for GET /api/v2/pos/registers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/registers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0798",
    "title": "POST api/v2/pos/sessions",
    "description": "Smoke test for POST /api/v2/pos/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/sessions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0799",
    "title": "GET api/v2/pos/sessions",
    "description": "Smoke test for GET /api/v2/pos/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/sessions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0800",
    "title": "GET api/v2/pos/sessions/{session}",
    "description": "Smoke test for GET /api/v2/pos/sessions/{session}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/sessions/{session} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0801",
    "title": "PUT api/v2/pos/sessions/{session}/close",
    "description": "Smoke test for PUT /api/v2/pos/sessions/{session}/close. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/pos/sessions/{session}/close once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0802",
    "title": "POST api/v2/pos/terminals",
    "description": "Smoke test for POST /api/v2/pos/terminals. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/terminals once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0803",
    "title": "POST api/v2/pos/terminals/{register}/toggle-status",
    "description": "Smoke test for POST /api/v2/pos/terminals/{register}/toggle-status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/terminals/{register}/toggle-status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0804",
    "title": "GET api/v2/pos/terminals",
    "description": "Smoke test for GET /api/v2/pos/terminals. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/terminals once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0805",
    "title": "GET api/v2/pos/terminals/{register}",
    "description": "Smoke test for GET /api/v2/pos/terminals/{register}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/terminals/{register} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0806",
    "title": "PUT api/v2/pos/terminals/{register}",
    "description": "Smoke test for PUT /api/v2/pos/terminals/{register}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/pos/terminals/{register} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0807",
    "title": "DELETE api/v2/pos/terminals/{register}",
    "description": "Smoke test for DELETE /api/v2/pos/terminals/{register}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/pos/terminals/{register} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0808",
    "title": "POST api/v2/pos/transactions",
    "description": "Smoke test for POST /api/v2/pos/transactions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/transactions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0809",
    "title": "POST api/v2/pos/transactions/{transaction}/void",
    "description": "Smoke test for POST /api/v2/pos/transactions/{transaction}/void. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/transactions/{transaction}/void once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0810",
    "title": "GET api/v2/pos/transactions",
    "description": "Smoke test for GET /api/v2/pos/transactions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/transactions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0811",
    "title": "GET api/v2/pos/transactions/{transaction}",
    "description": "Smoke test for GET /api/v2/pos/transactions/{transaction}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/transactions/{transaction} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0812",
    "title": "GET api/v2/pos/transactions/by-number/{number}",
    "description": "Smoke test for GET /api/v2/pos/transactions/by-number/{number}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pos/transactions/by-number/{number} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0813",
    "title": "POST api/v2/pos/transactions/return",
    "description": "Smoke test for POST /api/v2/pos/transactions/return. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/pos/transactions/return once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0814",
    "title": "POST api/v2/predefined-catalog/categories",
    "description": "Smoke test for POST /api/v2/predefined-catalog/categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/predefined-catalog/categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0815",
    "title": "POST api/v2/predefined-catalog/categories/{id}/clone",
    "description": "Smoke test for POST /api/v2/predefined-catalog/categories/{id}/clone. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/predefined-catalog/categories/{id}/clone once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0816",
    "title": "GET api/v2/predefined-catalog/categories",
    "description": "Smoke test for GET /api/v2/predefined-catalog/categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/predefined-catalog/categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0817",
    "title": "GET api/v2/predefined-catalog/categories/{id}",
    "description": "Smoke test for GET /api/v2/predefined-catalog/categories/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/predefined-catalog/categories/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0818",
    "title": "PUT api/v2/predefined-catalog/categories/{id}",
    "description": "Smoke test for PUT /api/v2/predefined-catalog/categories/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/predefined-catalog/categories/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0819",
    "title": "DELETE api/v2/predefined-catalog/categories/{id}",
    "description": "Smoke test for DELETE /api/v2/predefined-catalog/categories/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/predefined-catalog/categories/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0820",
    "title": "GET api/v2/predefined-catalog/categories/tree",
    "description": "Smoke test for GET /api/v2/predefined-catalog/categories/tree. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/predefined-catalog/categories/tree once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0821",
    "title": "POST api/v2/predefined-catalog/clone-all",
    "description": "Smoke test for POST /api/v2/predefined-catalog/clone-all. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/predefined-catalog/clone-all once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0822",
    "title": "POST api/v2/predefined-catalog/products",
    "description": "Smoke test for POST /api/v2/predefined-catalog/products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/predefined-catalog/products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0823",
    "title": "POST api/v2/predefined-catalog/products/{id}/clone",
    "description": "Smoke test for POST /api/v2/predefined-catalog/products/{id}/clone. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/predefined-catalog/products/{id}/clone once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0824",
    "title": "GET api/v2/predefined-catalog/products",
    "description": "Smoke test for GET /api/v2/predefined-catalog/products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/predefined-catalog/products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0825",
    "title": "GET api/v2/predefined-catalog/products/{id}",
    "description": "Smoke test for GET /api/v2/predefined-catalog/products/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/predefined-catalog/products/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0826",
    "title": "PUT api/v2/predefined-catalog/products/{id}",
    "description": "Smoke test for PUT /api/v2/predefined-catalog/products/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/predefined-catalog/products/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0827",
    "title": "DELETE api/v2/predefined-catalog/products/{id}",
    "description": "Smoke test for DELETE /api/v2/predefined-catalog/products/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/predefined-catalog/products/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0828",
    "title": "POST api/v2/predefined-catalog/products/bulk-action",
    "description": "Smoke test for POST /api/v2/predefined-catalog/products/bulk-action. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/predefined-catalog/products/bulk-action once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0829",
    "title": "GET api/v2/pricing",
    "description": "Smoke test for GET /api/v2/pricing. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pricing without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0830",
    "title": "GET api/v2/pricing/{planSlug}",
    "description": "Smoke test for GET /api/v2/pricing/{planSlug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pricing/{planSlug} without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0831",
    "title": "GET api/v2/pricing/plan/{planId}",
    "description": "Smoke test for GET /api/v2/pricing/plan/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/pricing/plan/{planId} without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0832",
    "title": "POST api/v2/promotions",
    "description": "Smoke test for POST /api/v2/promotions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/promotions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0833",
    "title": "POST api/v2/promotions/{promotion}/generate-coupons",
    "description": "Smoke test for POST /api/v2/promotions/{promotion}/generate-coupons. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/promotions/{promotion}/generate-coupons once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0834",
    "title": "POST api/v2/promotions/{promotion}/toggle",
    "description": "Smoke test for POST /api/v2/promotions/{promotion}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/promotions/{promotion}/toggle once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0835",
    "title": "GET api/v2/promotions",
    "description": "Smoke test for GET /api/v2/promotions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/promotions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0836",
    "title": "GET api/v2/promotions/{promotion}",
    "description": "Smoke test for GET /api/v2/promotions/{promotion}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/promotions/{promotion} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0837",
    "title": "GET api/v2/promotions/{promotion}/analytics",
    "description": "Smoke test for GET /api/v2/promotions/{promotion}/analytics. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/promotions/{promotion}/analytics once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0838",
    "title": "PUT api/v2/promotions/{promotion}",
    "description": "Smoke test for PUT /api/v2/promotions/{promotion}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/promotions/{promotion} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0839",
    "title": "DELETE api/v2/promotions/{promotion}",
    "description": "Smoke test for DELETE /api/v2/promotions/{promotion}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/promotions/{promotion} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0840",
    "title": "POST api/v2/provider-payments/{id}/resend-email",
    "description": "Smoke test for POST /api/v2/provider-payments/{id}/resend-email. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/provider-payments/{id}/resend-email once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0841",
    "title": "GET api/v2/provider-payments",
    "description": "Smoke test for GET /api/v2/provider-payments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/provider-payments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0842",
    "title": "GET api/v2/provider-payments/{id}",
    "description": "Smoke test for GET /api/v2/provider-payments/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/provider-payments/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0843",
    "title": "POST api/v2/provider-payments/initiate",
    "description": "Smoke test for POST /api/v2/provider-payments/initiate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/provider-payments/initiate once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0844",
    "title": "POST api/v2/provider-payments/ipn",
    "description": "Smoke test for POST /api/v2/provider-payments/ipn. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/provider-payments/ipn without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0845",
    "title": "GET api/v2/provider-payments/return",
    "description": "Smoke test for GET /api/v2/provider-payments/return. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/provider-payments/return without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0846",
    "title": "GET api/v2/provider-payments/statistics",
    "description": "Smoke test for GET /api/v2/provider-payments/statistics. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/provider-payments/statistics once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0847",
    "title": "GET api/v2/reports/category-breakdown",
    "description": "Smoke test for GET /api/v2/reports/category-breakdown. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/category-breakdown once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0848",
    "title": "GET api/v2/reports/customers/retention",
    "description": "Smoke test for GET /api/v2/reports/customers/retention. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/customers/retention once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0849",
    "title": "GET api/v2/reports/customers/top",
    "description": "Smoke test for GET /api/v2/reports/customers/top. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/customers/top once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0850",
    "title": "GET api/v2/reports/dashboard",
    "description": "Smoke test for GET /api/v2/reports/dashboard. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/dashboard once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0851",
    "title": "POST api/v2/reports/export",
    "description": "Smoke test for POST /api/v2/reports/export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/reports/export once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0852",
    "title": "GET api/v2/reports/financial/cash-variance",
    "description": "Smoke test for GET /api/v2/reports/financial/cash-variance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/financial/cash-variance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0853",
    "title": "GET api/v2/reports/financial/daily-pl",
    "description": "Smoke test for GET /api/v2/reports/financial/daily-pl. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/financial/daily-pl once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0854",
    "title": "GET api/v2/reports/financial/expenses",
    "description": "Smoke test for GET /api/v2/reports/financial/expenses. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/financial/expenses once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0855",
    "title": "GET api/v2/reports/hourly-sales",
    "description": "Smoke test for GET /api/v2/reports/hourly-sales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/hourly-sales once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0856",
    "title": "GET api/v2/reports/inventory/low-stock",
    "description": "Smoke test for GET /api/v2/reports/inventory/low-stock. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/inventory/low-stock once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0857",
    "title": "GET api/v2/reports/inventory/shrinkage",
    "description": "Smoke test for GET /api/v2/reports/inventory/shrinkage. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/inventory/shrinkage once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0858",
    "title": "GET api/v2/reports/inventory/turnover",
    "description": "Smoke test for GET /api/v2/reports/inventory/turnover. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/inventory/turnover once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0859",
    "title": "GET api/v2/reports/inventory/valuation",
    "description": "Smoke test for GET /api/v2/reports/inventory/valuation. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/inventory/valuation once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0860",
    "title": "GET api/v2/reports/payment-methods",
    "description": "Smoke test for GET /api/v2/reports/payment-methods. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/payment-methods once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0861",
    "title": "GET api/v2/reports/product-performance",
    "description": "Smoke test for GET /api/v2/reports/product-performance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/product-performance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0862",
    "title": "GET api/v2/reports/products/margin",
    "description": "Smoke test for GET /api/v2/reports/products/margin. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/products/margin once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0863",
    "title": "GET api/v2/reports/products/slow-movers",
    "description": "Smoke test for GET /api/v2/reports/products/slow-movers. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/products/slow-movers once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0864",
    "title": "POST api/v2/reports/refresh-summaries",
    "description": "Smoke test for POST /api/v2/reports/refresh-summaries. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/reports/refresh-summaries once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0865",
    "title": "GET api/v2/reports/sales-summary",
    "description": "Smoke test for GET /api/v2/reports/sales-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/sales-summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0866",
    "title": "POST api/v2/reports/schedules",
    "description": "Smoke test for POST /api/v2/reports/schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/reports/schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0867",
    "title": "GET api/v2/reports/schedules",
    "description": "Smoke test for GET /api/v2/reports/schedules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/schedules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0868",
    "title": "DELETE api/v2/reports/schedules/{id}",
    "description": "Smoke test for DELETE /api/v2/reports/schedules/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/reports/schedules/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0869",
    "title": "GET api/v2/reports/staff-performance",
    "description": "Smoke test for GET /api/v2/reports/staff-performance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/reports/staff-performance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0870",
    "title": "POST api/v2/security/audit-logs",
    "description": "Smoke test for POST /api/v2/security/audit-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/security/audit-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0871",
    "title": "GET api/v2/security/audit-logs",
    "description": "Smoke test for GET /api/v2/security/audit-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/audit-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0872",
    "title": "GET api/v2/security/audit-stats",
    "description": "Smoke test for GET /api/v2/security/audit-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/audit-stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0873",
    "title": "POST api/v2/security/devices",
    "description": "Smoke test for POST /api/v2/security/devices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/security/devices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0874",
    "title": "GET api/v2/security/devices",
    "description": "Smoke test for GET /api/v2/security/devices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/devices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0875",
    "title": "GET api/v2/security/devices/{id}",
    "description": "Smoke test for GET /api/v2/security/devices/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/devices/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0876",
    "title": "PUT api/v2/security/devices/{id}/deactivate",
    "description": "Smoke test for PUT /api/v2/security/devices/{id}/deactivate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/devices/{id}/deactivate once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0877",
    "title": "PUT api/v2/security/devices/{id}/heartbeat",
    "description": "Smoke test for PUT /api/v2/security/devices/{id}/heartbeat. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/devices/{id}/heartbeat once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0878",
    "title": "PUT api/v2/security/devices/{id}/remote-wipe",
    "description": "Smoke test for PUT /api/v2/security/devices/{id}/remote-wipe. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/devices/{id}/remote-wipe once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0879",
    "title": "POST api/v2/security/incidents",
    "description": "Smoke test for POST /api/v2/security/incidents. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/security/incidents once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0880",
    "title": "GET api/v2/security/incidents",
    "description": "Smoke test for GET /api/v2/security/incidents. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/incidents once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0881",
    "title": "PUT api/v2/security/incidents/{id}/resolve",
    "description": "Smoke test for PUT /api/v2/security/incidents/{id}/resolve. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/incidents/{id}/resolve once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0882",
    "title": "POST api/v2/security/login-attempts",
    "description": "Smoke test for POST /api/v2/security/login-attempts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/security/login-attempts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0883",
    "title": "GET api/v2/security/login-attempts",
    "description": "Smoke test for GET /api/v2/security/login-attempts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/login-attempts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0884",
    "title": "GET api/v2/security/login-attempts/failed-count",
    "description": "Smoke test for GET /api/v2/security/login-attempts/failed-count. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/login-attempts/failed-count once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0885",
    "title": "GET api/v2/security/login-attempts/is-locked-out",
    "description": "Smoke test for GET /api/v2/security/login-attempts/is-locked-out. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/login-attempts/is-locked-out once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0886",
    "title": "GET api/v2/security/login-attempts/stats",
    "description": "Smoke test for GET /api/v2/security/login-attempts/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/login-attempts/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0887",
    "title": "GET api/v2/security/overview",
    "description": "Smoke test for GET /api/v2/security/overview. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/overview once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0888",
    "title": "GET api/v2/security/policy",
    "description": "Smoke test for GET /api/v2/security/policy. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/policy once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0889",
    "title": "PUT api/v2/security/policy",
    "description": "Smoke test for PUT /api/v2/security/policy. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/policy once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0890",
    "title": "POST api/v2/security/sessions",
    "description": "Smoke test for POST /api/v2/security/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/security/sessions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0891",
    "title": "GET api/v2/security/sessions",
    "description": "Smoke test for GET /api/v2/security/sessions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/security/sessions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0892",
    "title": "PUT api/v2/security/sessions/{id}/end",
    "description": "Smoke test for PUT /api/v2/security/sessions/{id}/end. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/sessions/{id}/end once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0893",
    "title": "PUT api/v2/security/sessions/{id}/heartbeat",
    "description": "Smoke test for PUT /api/v2/security/sessions/{id}/heartbeat. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/security/sessions/{id}/heartbeat once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0894",
    "title": "POST api/v2/security/sessions/end-all",
    "description": "Smoke test for POST /api/v2/security/sessions/end-all. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/security/sessions/end-all once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0895",
    "title": "GET api/v2/settings/export-translations",
    "description": "Smoke test for GET /api/v2/settings/export-translations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/settings/export-translations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0896",
    "title": "POST api/v2/settings/locales",
    "description": "Smoke test for POST /api/v2/settings/locales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/settings/locales once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0897",
    "title": "GET api/v2/settings/locales",
    "description": "Smoke test for GET /api/v2/settings/locales. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/settings/locales once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0898",
    "title": "POST api/v2/settings/publish-translations",
    "description": "Smoke test for POST /api/v2/settings/publish-translations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/settings/publish-translations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0899",
    "title": "POST api/v2/settings/translation-overrides",
    "description": "Smoke test for POST /api/v2/settings/translation-overrides. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/settings/translation-overrides once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0900",
    "title": "GET api/v2/settings/translation-overrides",
    "description": "Smoke test for GET /api/v2/settings/translation-overrides. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/settings/translation-overrides once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0901",
    "title": "DELETE api/v2/settings/translation-overrides/{id}",
    "description": "Smoke test for DELETE /api/v2/settings/translation-overrides/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/settings/translation-overrides/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0902",
    "title": "GET api/v2/settings/translation-versions",
    "description": "Smoke test for GET /api/v2/settings/translation-versions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/settings/translation-versions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0903",
    "title": "POST api/v2/settings/translations",
    "description": "Smoke test for POST /api/v2/settings/translations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/settings/translations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0904",
    "title": "GET api/v2/settings/translations",
    "description": "Smoke test for GET /api/v2/settings/translations. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/settings/translations once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0905",
    "title": "POST api/v2/settings/translations/bulk-import",
    "description": "Smoke test for POST /api/v2/settings/translations/bulk-import. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/settings/translations/bulk-import once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0906",
    "title": "POST api/v2/signage/playlists",
    "description": "Smoke test for POST /api/v2/signage/playlists. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/signage/playlists once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0907",
    "title": "GET api/v2/signage/playlists",
    "description": "Smoke test for GET /api/v2/signage/playlists. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/signage/playlists once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0908",
    "title": "PUT api/v2/signage/playlists/{id}",
    "description": "Smoke test for PUT /api/v2/signage/playlists/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/signage/playlists/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0909",
    "title": "DELETE api/v2/signage/playlists/{id}",
    "description": "Smoke test for DELETE /api/v2/signage/playlists/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/signage/playlists/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0910",
    "title": "GET api/v2/staff/attendance",
    "description": "Smoke test for GET /api/v2/staff/attendance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/attendance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0911",
    "title": "POST api/v2/staff/attendance/clock",
    "description": "Smoke test for POST /api/v2/staff/attendance/clock. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/attendance/clock once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0912",
    "title": "GET api/v2/staff/attendance/export",
    "description": "Smoke test for GET /api/v2/staff/attendance/export. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/attendance/export once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0913",
    "title": "POST api/v2/staff/members",
    "description": "Smoke test for POST /api/v2/staff/members. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/members once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0914",
    "title": "POST api/v2/staff/members/{id}/branch-assignments",
    "description": "Smoke test for POST /api/v2/staff/members/{id}/branch-assignments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/members/{id}/branch-assignments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0915",
    "title": "POST api/v2/staff/members/{id}/link-user",
    "description": "Smoke test for POST /api/v2/staff/members/{id}/link-user. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/members/{id}/link-user once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0916",
    "title": "POST api/v2/staff/members/{id}/nfc",
    "description": "Smoke test for POST /api/v2/staff/members/{id}/nfc. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/members/{id}/nfc once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0917",
    "title": "POST api/v2/staff/members/{id}/pin",
    "description": "Smoke test for POST /api/v2/staff/members/{id}/pin. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/members/{id}/pin once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0918",
    "title": "GET api/v2/staff/members",
    "description": "Smoke test for GET /api/v2/staff/members. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0919",
    "title": "GET api/v2/staff/members/{id}",
    "description": "Smoke test for GET /api/v2/staff/members/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0920",
    "title": "GET api/v2/staff/members/{id}/activity-log",
    "description": "Smoke test for GET /api/v2/staff/members/{id}/activity-log. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members/{id}/activity-log once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0921",
    "title": "GET api/v2/staff/members/{id}/branch-assignments",
    "description": "Smoke test for GET /api/v2/staff/members/{id}/branch-assignments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members/{id}/branch-assignments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0922",
    "title": "GET api/v2/staff/members/{id}/commissions",
    "description": "Smoke test for GET /api/v2/staff/members/{id}/commissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members/{id}/commissions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0923",
    "title": "PUT api/v2/staff/members/{id}",
    "description": "Smoke test for PUT /api/v2/staff/members/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/staff/members/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0924",
    "title": "PUT api/v2/staff/members/{id}/commission-config",
    "description": "Smoke test for PUT /api/v2/staff/members/{id}/commission-config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/staff/members/{id}/commission-config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0925",
    "title": "DELETE api/v2/staff/members/{id}",
    "description": "Smoke test for DELETE /api/v2/staff/members/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/staff/members/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0926",
    "title": "DELETE api/v2/staff/members/{id}/branch-assignments",
    "description": "Smoke test for DELETE /api/v2/staff/members/{id}/branch-assignments. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/staff/members/{id}/branch-assignments once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0927",
    "title": "DELETE api/v2/staff/members/{id}/link-user",
    "description": "Smoke test for DELETE /api/v2/staff/members/{id}/link-user. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/staff/members/{id}/link-user once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0928",
    "title": "GET api/v2/staff/members/linkable-users",
    "description": "Smoke test for GET /api/v2/staff/members/linkable-users. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members/linkable-users once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0929",
    "title": "GET api/v2/staff/members/stats",
    "description": "Smoke test for GET /api/v2/staff/members/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/members/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0930",
    "title": "GET api/v2/staff/permissions",
    "description": "Smoke test for GET /api/v2/staff/permissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/permissions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0931",
    "title": "GET api/v2/staff/permissions/grouped",
    "description": "Smoke test for GET /api/v2/staff/permissions/grouped. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/permissions/grouped once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0932",
    "title": "GET api/v2/staff/permissions/module/{module}",
    "description": "Smoke test for GET /api/v2/staff/permissions/module/{module}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/permissions/module/{module} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0933",
    "title": "GET api/v2/staff/permissions/modules",
    "description": "Smoke test for GET /api/v2/staff/permissions/modules. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/permissions/modules once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0934",
    "title": "GET api/v2/staff/permissions/pin-protected",
    "description": "Smoke test for GET /api/v2/staff/permissions/pin-protected. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/permissions/pin-protected once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0935",
    "title": "POST api/v2/staff/pin-override",
    "description": "Smoke test for POST /api/v2/staff/pin-override. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/pin-override once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0936",
    "title": "GET api/v2/staff/pin-override/check",
    "description": "Smoke test for GET /api/v2/staff/pin-override/check. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/pin-override/check once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0937",
    "title": "GET api/v2/staff/pin-override/history",
    "description": "Smoke test for GET /api/v2/staff/pin-override/history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/pin-override/history once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0938",
    "title": "POST api/v2/staff/roles",
    "description": "Smoke test for POST /api/v2/staff/roles. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/roles once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0939",
    "title": "POST api/v2/staff/roles/{role}/assign",
    "description": "Smoke test for POST /api/v2/staff/roles/{role}/assign. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/roles/{role}/assign once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0940",
    "title": "POST api/v2/staff/roles/{role}/unassign",
    "description": "Smoke test for POST /api/v2/staff/roles/{role}/unassign. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/roles/{role}/unassign once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0941",
    "title": "GET api/v2/staff/roles",
    "description": "Smoke test for GET /api/v2/staff/roles. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/roles once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0942",
    "title": "GET api/v2/staff/roles/{role}",
    "description": "Smoke test for GET /api/v2/staff/roles/{role}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/roles/{role} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0943",
    "title": "PUT api/v2/staff/roles/{role}",
    "description": "Smoke test for PUT /api/v2/staff/roles/{role}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/staff/roles/{role} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0944",
    "title": "DELETE api/v2/staff/roles/{role}",
    "description": "Smoke test for DELETE /api/v2/staff/roles/{role}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/staff/roles/{role} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0945",
    "title": "GET api/v2/staff/roles/user-permissions",
    "description": "Smoke test for GET /api/v2/staff/roles/user-permissions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/roles/user-permissions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0946",
    "title": "POST api/v2/staff/shift-templates",
    "description": "Smoke test for POST /api/v2/staff/shift-templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/shift-templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0947",
    "title": "GET api/v2/staff/shift-templates",
    "description": "Smoke test for GET /api/v2/staff/shift-templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/shift-templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0948",
    "title": "POST api/v2/staff/shifts",
    "description": "Smoke test for POST /api/v2/staff/shifts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/staff/shifts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0949",
    "title": "GET api/v2/staff/shifts",
    "description": "Smoke test for GET /api/v2/staff/shifts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/staff/shifts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0950",
    "title": "PUT api/v2/staff/shifts/{id}",
    "description": "Smoke test for PUT /api/v2/staff/shifts/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/staff/shifts/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0951",
    "title": "DELETE api/v2/staff/shifts/{id}",
    "description": "Smoke test for DELETE /api/v2/staff/shifts/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/staff/shifts/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0952",
    "title": "GET api/v2/subscription/add-ons",
    "description": "Smoke test for GET /api/v2/subscription/add-ons. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/add-ons without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0953",
    "title": "POST api/v2/subscription/cancel",
    "description": "Smoke test for POST /api/v2/subscription/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/subscription/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0954",
    "title": "PUT api/v2/subscription/change-plan",
    "description": "Smoke test for PUT /api/v2/subscription/change-plan. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/subscription/change-plan once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0955",
    "title": "GET api/v2/subscription/check-feature/{featureKey}",
    "description": "Smoke test for GET /api/v2/subscription/check-feature/{featureKey}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/check-feature/{featureKey} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0956",
    "title": "GET api/v2/subscription/check-limit/{limitKey}",
    "description": "Smoke test for GET /api/v2/subscription/check-limit/{limitKey}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/check-limit/{limitKey} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0957",
    "title": "GET api/v2/subscription/current",
    "description": "Smoke test for GET /api/v2/subscription/current. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/current once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0958",
    "title": "GET api/v2/subscription/invoices",
    "description": "Smoke test for GET /api/v2/subscription/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/invoices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0959",
    "title": "GET api/v2/subscription/invoices/{invoiceId}",
    "description": "Smoke test for GET /api/v2/subscription/invoices/{invoiceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/invoices/{invoiceId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0960",
    "title": "GET api/v2/subscription/invoices/{invoiceId}/pdf",
    "description": "Smoke test for GET /api/v2/subscription/invoices/{invoiceId}/pdf. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/invoices/{invoiceId}/pdf once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0961",
    "title": "POST api/v2/subscription/plans",
    "description": "Smoke test for POST /api/v2/subscription/plans. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/subscription/plans once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0962",
    "title": "GET api/v2/subscription/plans",
    "description": "Smoke test for GET /api/v2/subscription/plans. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/plans without auth. Expect status < 500. Public route."
  },
  {
    "id": "TC0963",
    "title": "GET api/v2/subscription/plans/{planId}",
    "description": "Smoke test for GET /api/v2/subscription/plans/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/plans/{planId} without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0964",
    "title": "PUT api/v2/subscription/plans/{planId}",
    "description": "Smoke test for PUT /api/v2/subscription/plans/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/subscription/plans/{planId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0965",
    "title": "PATCH api/v2/subscription/plans/{planId}/toggle",
    "description": "Smoke test for PATCH /api/v2/subscription/plans/{planId}/toggle. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/subscription/plans/{planId}/toggle once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0966",
    "title": "DELETE api/v2/subscription/plans/{planId}",
    "description": "Smoke test for DELETE /api/v2/subscription/plans/{planId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/subscription/plans/{planId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0967",
    "title": "POST api/v2/subscription/plans/compare",
    "description": "Smoke test for POST /api/v2/subscription/plans/compare. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/subscription/plans/compare without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0968",
    "title": "GET api/v2/subscription/plans/slug/{slug}",
    "description": "Smoke test for GET /api/v2/subscription/plans/slug/{slug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/plans/slug/{slug} without auth. Expect status < 500. Public route.  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0969",
    "title": "POST api/v2/subscription/resume",
    "description": "Smoke test for POST /api/v2/subscription/resume. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/subscription/resume once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0970",
    "title": "GET api/v2/subscription/store-add-ons",
    "description": "Smoke test for GET /api/v2/subscription/store-add-ons. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/store-add-ons once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0971",
    "title": "POST api/v2/subscription/subscribe",
    "description": "Smoke test for POST /api/v2/subscription/subscribe. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/subscription/subscribe once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0972",
    "title": "GET api/v2/subscription/sync/entitlements",
    "description": "Smoke test for GET /api/v2/subscription/sync/entitlements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/sync/entitlements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0973",
    "title": "GET api/v2/subscription/usage",
    "description": "Smoke test for GET /api/v2/subscription/usage. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/subscription/usage once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0974",
    "title": "GET api/v2/support/kb",
    "description": "Smoke test for GET /api/v2/support/kb. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/support/kb once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0975",
    "title": "GET api/v2/support/kb/{slug}",
    "description": "Smoke test for GET /api/v2/support/kb/{slug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/support/kb/{slug} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0976",
    "title": "GET api/v2/support/stats",
    "description": "Smoke test for GET /api/v2/support/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/support/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0977",
    "title": "POST api/v2/support/tickets",
    "description": "Smoke test for POST /api/v2/support/tickets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/support/tickets once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0978",
    "title": "POST api/v2/support/tickets/{id}/messages",
    "description": "Smoke test for POST /api/v2/support/tickets/{id}/messages. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/support/tickets/{id}/messages once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0979",
    "title": "GET api/v2/support/tickets",
    "description": "Smoke test for GET /api/v2/support/tickets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/support/tickets once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0980",
    "title": "GET api/v2/support/tickets/{id}",
    "description": "Smoke test for GET /api/v2/support/tickets/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/support/tickets/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC0981",
    "title": "PUT api/v2/support/tickets/{id}/close",
    "description": "Smoke test for PUT /api/v2/support/tickets/{id}/close. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/support/tickets/{id}/close once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0982",
    "title": "GET api/v2/sync/conflicts",
    "description": "Smoke test for GET /api/v2/sync/conflicts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/sync/conflicts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0983",
    "title": "GET api/v2/sync/full",
    "description": "Smoke test for GET /api/v2/sync/full. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/sync/full once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0984",
    "title": "POST api/v2/sync/heartbeat",
    "description": "Smoke test for POST /api/v2/sync/heartbeat. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/sync/heartbeat once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0985",
    "title": "GET api/v2/sync/pull",
    "description": "Smoke test for GET /api/v2/sync/pull. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/sync/pull once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0986",
    "title": "POST api/v2/sync/push",
    "description": "Smoke test for POST /api/v2/sync/push. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/sync/push once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0987",
    "title": "POST api/v2/sync/resolve-conflict/{conflictId}",
    "description": "Smoke test for POST /api/v2/sync/resolve-conflict/{conflictId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/sync/resolve-conflict/{conflictId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0988",
    "title": "GET api/v2/sync/status",
    "description": "Smoke test for GET /api/v2/sync/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/sync/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0989",
    "title": "GET api/v2/thawani/category-mappings",
    "description": "Smoke test for GET /api/v2/thawani/category-mappings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/category-mappings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0990",
    "title": "GET api/v2/thawani/column-mappings",
    "description": "Smoke test for GET /api/v2/thawani/column-mappings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/column-mappings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0991",
    "title": "POST api/v2/thawani/column-mappings/seed-defaults",
    "description": "Smoke test for POST /api/v2/thawani/column-mappings/seed-defaults. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/column-mappings/seed-defaults once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0992",
    "title": "POST api/v2/thawani/config",
    "description": "Smoke test for POST /api/v2/thawani/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0993",
    "title": "GET api/v2/thawani/config",
    "description": "Smoke test for GET /api/v2/thawani/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0994",
    "title": "PUT api/v2/thawani/disconnect",
    "description": "Smoke test for PUT /api/v2/thawani/disconnect. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/thawani/disconnect once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0995",
    "title": "GET api/v2/thawani/orders",
    "description": "Smoke test for GET /api/v2/thawani/orders. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/orders once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0996",
    "title": "POST api/v2/thawani/process-queue",
    "description": "Smoke test for POST /api/v2/thawani/process-queue. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/process-queue once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0997",
    "title": "GET api/v2/thawani/product-mappings",
    "description": "Smoke test for GET /api/v2/thawani/product-mappings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/product-mappings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC0998",
    "title": "POST api/v2/thawani/pull-categories",
    "description": "Smoke test for POST /api/v2/thawani/pull-categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/pull-categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC0999",
    "title": "POST api/v2/thawani/pull-products",
    "description": "Smoke test for POST /api/v2/thawani/pull-products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/pull-products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1000",
    "title": "POST api/v2/thawani/push-categories",
    "description": "Smoke test for POST /api/v2/thawani/push-categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/push-categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1001",
    "title": "POST api/v2/thawani/push-products",
    "description": "Smoke test for POST /api/v2/thawani/push-products. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/push-products once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1002",
    "title": "GET api/v2/thawani/queue-stats",
    "description": "Smoke test for GET /api/v2/thawani/queue-stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/queue-stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1003",
    "title": "GET api/v2/thawani/settlements",
    "description": "Smoke test for GET /api/v2/thawani/settlements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/settlements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1004",
    "title": "GET api/v2/thawani/stats",
    "description": "Smoke test for GET /api/v2/thawani/stats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/stats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1005",
    "title": "GET api/v2/thawani/sync-logs",
    "description": "Smoke test for GET /api/v2/thawani/sync-logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/thawani/sync-logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1006",
    "title": "POST api/v2/thawani/test-connection",
    "description": "Smoke test for POST /api/v2/thawani/test-connection. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/thawani/test-connection once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1007",
    "title": "GET api/v2/ui/cfd-themes",
    "description": "Smoke test for GET /api/v2/ui/cfd-themes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/cfd-themes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1008",
    "title": "GET api/v2/ui/cfd-themes/{id}/preview-url",
    "description": "Smoke test for GET /api/v2/ui/cfd-themes/{id}/preview-url. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/cfd-themes/{id}/preview-url once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1009",
    "title": "GET api/v2/ui/cfd-themes/{slug}",
    "description": "Smoke test for GET /api/v2/ui/cfd-themes/{slug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/cfd-themes/{slug} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1010",
    "title": "GET api/v2/ui/defaults",
    "description": "Smoke test for GET /api/v2/ui/defaults. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/defaults once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1011",
    "title": "GET api/v2/ui/label-templates",
    "description": "Smoke test for GET /api/v2/ui/label-templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/label-templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1012",
    "title": "GET api/v2/ui/label-templates/{id}/preview-url",
    "description": "Smoke test for GET /api/v2/ui/label-templates/{id}/preview-url. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/label-templates/{id}/preview-url once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1013",
    "title": "GET api/v2/ui/label-templates/{slug}",
    "description": "Smoke test for GET /api/v2/ui/label-templates/{slug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/label-templates/{slug} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1014",
    "title": "GET api/v2/ui/layout-builder/canvas",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/canvas. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/canvas once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1015",
    "title": "PUT api/v2/ui/layout-builder/canvas",
    "description": "Smoke test for PUT /api/v2/ui/layout-builder/canvas. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/layout-builder/canvas once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1016",
    "title": "POST api/v2/ui/layout-builder/clone",
    "description": "Smoke test for POST /api/v2/ui/layout-builder/clone. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/layout-builder/clone once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1017",
    "title": "GET api/v2/ui/layout-builder/full",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/full. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/full once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1018",
    "title": "POST api/v2/ui/layout-builder/placements",
    "description": "Smoke test for POST /api/v2/ui/layout-builder/placements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/layout-builder/placements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1019",
    "title": "GET api/v2/ui/layout-builder/placements",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/placements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/placements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1020",
    "title": "PUT api/v2/ui/layout-builder/placements/{placementId}",
    "description": "Smoke test for PUT /api/v2/ui/layout-builder/placements/{placementId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/layout-builder/placements/{placementId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1021",
    "title": "PUT api/v2/ui/layout-builder/placements/{placementId}/theme-overrides",
    "description": "Smoke test for PUT /api/v2/ui/layout-builder/placements/{placementId}/theme-overrides. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/layout-builder/placements/{placementId}/theme-overrides once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1022",
    "title": "DELETE api/v2/ui/layout-builder/placements/{placementId}",
    "description": "Smoke test for DELETE /api/v2/ui/layout-builder/placements/{placementId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/ui/layout-builder/placements/{placementId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1023",
    "title": "DELETE api/v2/ui/layout-builder/placements/{placementId}/theme-overrides/{variableKey}",
    "description": "Smoke test for DELETE /api/v2/ui/layout-builder/placements/{placementId}/theme-overrides/{variableKey}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/ui/layout-builder/placements/{placementId}/theme-overrides/{variableKey} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1024",
    "title": "POST api/v2/ui/layout-builder/templates/{templateId}/clone",
    "description": "Smoke test for POST /api/v2/ui/layout-builder/templates/{templateId}/clone. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/layout-builder/templates/{templateId}/clone once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1025",
    "title": "POST api/v2/ui/layout-builder/templates/{templateId}/placements",
    "description": "Smoke test for POST /api/v2/ui/layout-builder/templates/{templateId}/placements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/layout-builder/templates/{templateId}/placements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1026",
    "title": "POST api/v2/ui/layout-builder/templates/{templateId}/versions",
    "description": "Smoke test for POST /api/v2/ui/layout-builder/templates/{templateId}/versions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/layout-builder/templates/{templateId}/versions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1027",
    "title": "GET api/v2/ui/layout-builder/templates/{templateId}/canvas",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/templates/{templateId}/canvas. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/templates/{templateId}/canvas once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1028",
    "title": "GET api/v2/ui/layout-builder/templates/{templateId}/full",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/templates/{templateId}/full. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/templates/{templateId}/full once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1029",
    "title": "GET api/v2/ui/layout-builder/templates/{templateId}/placements",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/templates/{templateId}/placements. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/templates/{templateId}/placements once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1030",
    "title": "GET api/v2/ui/layout-builder/templates/{templateId}/versions",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/templates/{templateId}/versions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/templates/{templateId}/versions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1031",
    "title": "PUT api/v2/ui/layout-builder/templates/{templateId}/canvas",
    "description": "Smoke test for PUT /api/v2/ui/layout-builder/templates/{templateId}/canvas. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/layout-builder/templates/{templateId}/canvas once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1032",
    "title": "PUT api/v2/ui/layout-builder/templates/{templateId}/placements/batch",
    "description": "Smoke test for PUT /api/v2/ui/layout-builder/templates/{templateId}/placements/batch. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/layout-builder/templates/{templateId}/placements/batch once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1033",
    "title": "POST api/v2/ui/layout-builder/versions",
    "description": "Smoke test for POST /api/v2/ui/layout-builder/versions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/layout-builder/versions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1034",
    "title": "GET api/v2/ui/layout-builder/versions",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/versions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/versions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1035",
    "title": "GET api/v2/ui/layout-builder/widgets",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/widgets. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/widgets once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1036",
    "title": "GET api/v2/ui/layout-builder/widgets/{id}",
    "description": "Smoke test for GET /api/v2/ui/layout-builder/widgets/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layout-builder/widgets/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1037",
    "title": "GET api/v2/ui/layouts",
    "description": "Smoke test for GET /api/v2/ui/layouts. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/layouts once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1038",
    "title": "GET api/v2/ui/marketplace/categories",
    "description": "Smoke test for GET /api/v2/ui/marketplace/categories. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/categories once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1039",
    "title": "GET api/v2/ui/marketplace/categories/{id}",
    "description": "Smoke test for GET /api/v2/ui/marketplace/categories/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/categories/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1040",
    "title": "GET api/v2/ui/marketplace/invoices/{invoiceId}",
    "description": "Smoke test for GET /api/v2/ui/marketplace/invoices/{invoiceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/invoices/{invoiceId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1041",
    "title": "POST api/v2/ui/marketplace/listings/{listingId}/purchase",
    "description": "Smoke test for POST /api/v2/ui/marketplace/listings/{listingId}/purchase. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/marketplace/listings/{listingId}/purchase once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1042",
    "title": "POST api/v2/ui/marketplace/listings/{listingId}/reviews",
    "description": "Smoke test for POST /api/v2/ui/marketplace/listings/{listingId}/reviews. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/marketplace/listings/{listingId}/reviews once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1043",
    "title": "GET api/v2/ui/marketplace/listings",
    "description": "Smoke test for GET /api/v2/ui/marketplace/listings. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/listings once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1044",
    "title": "GET api/v2/ui/marketplace/listings/{id}",
    "description": "Smoke test for GET /api/v2/ui/marketplace/listings/{id}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/listings/{id} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1045",
    "title": "GET api/v2/ui/marketplace/listings/{id}/preview-url",
    "description": "Smoke test for GET /api/v2/ui/marketplace/listings/{id}/preview-url. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/listings/{id}/preview-url once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1046",
    "title": "GET api/v2/ui/marketplace/listings/{listingId}/check-access",
    "description": "Smoke test for GET /api/v2/ui/marketplace/listings/{listingId}/check-access. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/listings/{listingId}/check-access once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1047",
    "title": "GET api/v2/ui/marketplace/listings/{listingId}/reviews",
    "description": "Smoke test for GET /api/v2/ui/marketplace/listings/{listingId}/reviews. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/listings/{listingId}/reviews once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1048",
    "title": "GET api/v2/ui/marketplace/my-invoices",
    "description": "Smoke test for GET /api/v2/ui/marketplace/my-invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/my-invoices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1049",
    "title": "GET api/v2/ui/marketplace/my-purchases",
    "description": "Smoke test for GET /api/v2/ui/marketplace/my-purchases. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/marketplace/my-purchases once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1050",
    "title": "POST api/v2/ui/marketplace/purchases/{purchaseId}/cancel",
    "description": "Smoke test for POST /api/v2/ui/marketplace/purchases/{purchaseId}/cancel. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/ui/marketplace/purchases/{purchaseId}/cancel once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1051",
    "title": "PUT api/v2/ui/marketplace/reviews/{reviewId}",
    "description": "Smoke test for PUT /api/v2/ui/marketplace/reviews/{reviewId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/marketplace/reviews/{reviewId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1052",
    "title": "DELETE api/v2/ui/marketplace/reviews/{reviewId}",
    "description": "Smoke test for DELETE /api/v2/ui/marketplace/reviews/{reviewId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/ui/marketplace/reviews/{reviewId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1053",
    "title": "GET api/v2/ui/preferences",
    "description": "Smoke test for GET /api/v2/ui/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1054",
    "title": "PUT api/v2/ui/preferences",
    "description": "Smoke test for PUT /api/v2/ui/preferences. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/preferences once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1055",
    "title": "GET api/v2/ui/receipt-templates",
    "description": "Smoke test for GET /api/v2/ui/receipt-templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/receipt-templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1056",
    "title": "GET api/v2/ui/receipt-templates/{id}/preview-url",
    "description": "Smoke test for GET /api/v2/ui/receipt-templates/{id}/preview-url. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/receipt-templates/{id}/preview-url once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1057",
    "title": "GET api/v2/ui/receipt-templates/{slug}",
    "description": "Smoke test for GET /api/v2/ui/receipt-templates/{slug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/receipt-templates/{slug} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1058",
    "title": "GET api/v2/ui/signage-templates",
    "description": "Smoke test for GET /api/v2/ui/signage-templates. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/signage-templates once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1059",
    "title": "GET api/v2/ui/signage-templates/{slug}",
    "description": "Smoke test for GET /api/v2/ui/signage-templates/{slug}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/signage-templates/{slug} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1060",
    "title": "PUT api/v2/ui/store-defaults",
    "description": "Smoke test for PUT /api/v2/ui/store-defaults. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/ui/store-defaults once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1061",
    "title": "GET api/v2/ui/themes",
    "description": "Smoke test for GET /api/v2/ui/themes. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/ui/themes once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1062",
    "title": "GET api/v2/wameed-ai/billing/invoices",
    "description": "Smoke test for GET /api/v2/wameed-ai/billing/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/billing/invoices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1063",
    "title": "GET api/v2/wameed-ai/billing/invoices/{invoiceId}",
    "description": "Smoke test for GET /api/v2/wameed-ai/billing/invoices/{invoiceId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/billing/invoices/{invoiceId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1064",
    "title": "GET api/v2/wameed-ai/billing/summary",
    "description": "Smoke test for GET /api/v2/wameed-ai/billing/summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/billing/summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1065",
    "title": "POST api/v2/wameed-ai/catalog/barcode-enrichment",
    "description": "Smoke test for POST /api/v2/wameed-ai/catalog/barcode-enrichment. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/catalog/barcode-enrichment once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1066",
    "title": "POST api/v2/wameed-ai/catalog/invoice-ocr",
    "description": "Smoke test for POST /api/v2/wameed-ai/catalog/invoice-ocr. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/catalog/invoice-ocr once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1067",
    "title": "POST api/v2/wameed-ai/catalog/product-categorization",
    "description": "Smoke test for POST /api/v2/wameed-ai/catalog/product-categorization. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/catalog/product-categorization once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1068",
    "title": "POST api/v2/wameed-ai/catalog/product-description",
    "description": "Smoke test for POST /api/v2/wameed-ai/catalog/product-description. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/catalog/product-description once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1069",
    "title": "POST api/v2/wameed-ai/chats",
    "description": "Smoke test for POST /api/v2/wameed-ai/chats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/chats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1070",
    "title": "POST api/v2/wameed-ai/chats/{chatId}/feature",
    "description": "Smoke test for POST /api/v2/wameed-ai/chats/{chatId}/feature. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/chats/{chatId}/feature once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1071",
    "title": "POST api/v2/wameed-ai/chats/{chatId}/messages",
    "description": "Smoke test for POST /api/v2/wameed-ai/chats/{chatId}/messages. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/chats/{chatId}/messages once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1072",
    "title": "GET api/v2/wameed-ai/chats",
    "description": "Smoke test for GET /api/v2/wameed-ai/chats. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/chats once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1073",
    "title": "GET api/v2/wameed-ai/chats/{chatId}",
    "description": "Smoke test for GET /api/v2/wameed-ai/chats/{chatId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/chats/{chatId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1074",
    "title": "PUT api/v2/wameed-ai/chats/{chatId}/model",
    "description": "Smoke test for PUT /api/v2/wameed-ai/chats/{chatId}/model. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/wameed-ai/chats/{chatId}/model once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1075",
    "title": "PUT api/v2/wameed-ai/chats/{chatId}/title",
    "description": "Smoke test for PUT /api/v2/wameed-ai/chats/{chatId}/title. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/wameed-ai/chats/{chatId}/title once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1076",
    "title": "DELETE api/v2/wameed-ai/chats/{chatId}",
    "description": "Smoke test for DELETE /api/v2/wameed-ai/chats/{chatId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/wameed-ai/chats/{chatId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1077",
    "title": "POST api/v2/wameed-ai/communication/marketing-generator",
    "description": "Smoke test for POST /api/v2/wameed-ai/communication/marketing-generator. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/communication/marketing-generator once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1078",
    "title": "POST api/v2/wameed-ai/communication/social-content",
    "description": "Smoke test for POST /api/v2/wameed-ai/communication/social-content. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/communication/social-content once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1079",
    "title": "POST api/v2/wameed-ai/communication/translate",
    "description": "Smoke test for POST /api/v2/wameed-ai/communication/translate. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/communication/translate once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1080",
    "title": "GET api/v2/wameed-ai/config",
    "description": "Smoke test for GET /api/v2/wameed-ai/config. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/config once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1081",
    "title": "PUT api/v2/wameed-ai/config/{featureId}",
    "description": "Smoke test for PUT /api/v2/wameed-ai/config/{featureId}. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PUT /api/v2/wameed-ai/config/{featureId} once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1082",
    "title": "POST api/v2/wameed-ai/customers/churn-prediction",
    "description": "Smoke test for POST /api/v2/wameed-ai/customers/churn-prediction. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/customers/churn-prediction once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1083",
    "title": "POST api/v2/wameed-ai/customers/personalized-promotions",
    "description": "Smoke test for POST /api/v2/wameed-ai/customers/personalized-promotions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/customers/personalized-promotions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1084",
    "title": "POST api/v2/wameed-ai/customers/segmentation",
    "description": "Smoke test for POST /api/v2/wameed-ai/customers/segmentation. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/customers/segmentation once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1085",
    "title": "POST api/v2/wameed-ai/customers/sentiment-analysis",
    "description": "Smoke test for POST /api/v2/wameed-ai/customers/sentiment-analysis. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/customers/sentiment-analysis once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1086",
    "title": "POST api/v2/wameed-ai/customers/spending-patterns",
    "description": "Smoke test for POST /api/v2/wameed-ai/customers/spending-patterns. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/customers/spending-patterns once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1087",
    "title": "GET api/v2/wameed-ai/features",
    "description": "Smoke test for GET /api/v2/wameed-ai/features. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/features once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1088",
    "title": "GET api/v2/wameed-ai/features/cards",
    "description": "Smoke test for GET /api/v2/wameed-ai/features/cards. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/features/cards once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1089",
    "title": "POST api/v2/wameed-ai/feedback",
    "description": "Smoke test for POST /api/v2/wameed-ai/feedback. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/feedback once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1090",
    "title": "POST api/v2/wameed-ai/financial/cashflow-prediction",
    "description": "Smoke test for POST /api/v2/wameed-ai/financial/cashflow-prediction. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/financial/cashflow-prediction once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1091",
    "title": "POST api/v2/wameed-ai/financial/expense-analysis",
    "description": "Smoke test for POST /api/v2/wameed-ai/financial/expense-analysis. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/financial/expense-analysis once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1092",
    "title": "POST api/v2/wameed-ai/financial/margin-analyzer",
    "description": "Smoke test for POST /api/v2/wameed-ai/financial/margin-analyzer. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/financial/margin-analyzer once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1093",
    "title": "POST api/v2/wameed-ai/inventory/dead-stock",
    "description": "Smoke test for POST /api/v2/wameed-ai/inventory/dead-stock. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/inventory/dead-stock once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1094",
    "title": "POST api/v2/wameed-ai/inventory/expiry-manager",
    "description": "Smoke test for POST /api/v2/wameed-ai/inventory/expiry-manager. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/inventory/expiry-manager once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1095",
    "title": "POST api/v2/wameed-ai/inventory/seasonal-planning",
    "description": "Smoke test for POST /api/v2/wameed-ai/inventory/seasonal-planning. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/inventory/seasonal-planning once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1096",
    "title": "POST api/v2/wameed-ai/inventory/shrinkage-detection",
    "description": "Smoke test for POST /api/v2/wameed-ai/inventory/shrinkage-detection. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/inventory/shrinkage-detection once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1097",
    "title": "POST api/v2/wameed-ai/inventory/smart-reorder",
    "description": "Smoke test for POST /api/v2/wameed-ai/inventory/smart-reorder. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/inventory/smart-reorder once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1098",
    "title": "POST api/v2/wameed-ai/inventory/supplier-analysis",
    "description": "Smoke test for POST /api/v2/wameed-ai/inventory/supplier-analysis. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/inventory/supplier-analysis once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1099",
    "title": "GET api/v2/wameed-ai/models",
    "description": "Smoke test for GET /api/v2/wameed-ai/models. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/models once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1100",
    "title": "POST api/v2/wameed-ai/operations/cashier-errors",
    "description": "Smoke test for POST /api/v2/wameed-ai/operations/cashier-errors. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/operations/cashier-errors once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1101",
    "title": "POST api/v2/wameed-ai/operations/efficiency-score",
    "description": "Smoke test for POST /api/v2/wameed-ai/operations/efficiency-score. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/operations/efficiency-score once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1102",
    "title": "POST api/v2/wameed-ai/operations/smart-search",
    "description": "Smoke test for POST /api/v2/wameed-ai/operations/smart-search. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/operations/smart-search once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1103",
    "title": "POST api/v2/wameed-ai/operations/staff-performance",
    "description": "Smoke test for POST /api/v2/wameed-ai/operations/staff-performance. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/operations/staff-performance once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1104",
    "title": "POST api/v2/wameed-ai/sales/bundle-suggestions",
    "description": "Smoke test for POST /api/v2/wameed-ai/sales/bundle-suggestions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/sales/bundle-suggestions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1105",
    "title": "POST api/v2/wameed-ai/sales/daily-summary",
    "description": "Smoke test for POST /api/v2/wameed-ai/sales/daily-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/sales/daily-summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1106",
    "title": "POST api/v2/wameed-ai/sales/forecast",
    "description": "Smoke test for POST /api/v2/wameed-ai/sales/forecast. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/sales/forecast once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1107",
    "title": "POST api/v2/wameed-ai/sales/peak-hours",
    "description": "Smoke test for POST /api/v2/wameed-ai/sales/peak-hours. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/sales/peak-hours once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1108",
    "title": "POST api/v2/wameed-ai/sales/pricing-optimization",
    "description": "Smoke test for POST /api/v2/wameed-ai/sales/pricing-optimization. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/sales/pricing-optimization once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1109",
    "title": "POST api/v2/wameed-ai/sales/revenue-anomaly",
    "description": "Smoke test for POST /api/v2/wameed-ai/sales/revenue-anomaly. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wameed-ai/sales/revenue-anomaly once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1110",
    "title": "GET api/v2/wameed-ai/suggestions",
    "description": "Smoke test for GET /api/v2/wameed-ai/suggestions. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/suggestions once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1111",
    "title": "PATCH api/v2/wameed-ai/suggestions/{suggestionId}/status",
    "description": "Smoke test for PATCH /api/v2/wameed-ai/suggestions/{suggestionId}/status. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call PATCH /api/v2/wameed-ai/suggestions/{suggestionId}/status once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1112",
    "title": "GET api/v2/wameed-ai/usage",
    "description": "Smoke test for GET /api/v2/wameed-ai/usage. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/usage once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1113",
    "title": "GET api/v2/wameed-ai/usage/history",
    "description": "Smoke test for GET /api/v2/wameed-ai/usage/history. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/usage/history once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1114",
    "title": "GET api/v2/wameed-ai/usage/logs",
    "description": "Smoke test for GET /api/v2/wameed-ai/usage/logs. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wameed-ai/usage/logs once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1115",
    "title": "POST api/v2/website/consultation",
    "description": "Smoke test for POST /api/v2/website/consultation. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/consultation without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1116",
    "title": "POST api/v2/website/contact",
    "description": "Smoke test for POST /api/v2/website/contact. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/contact without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1117",
    "title": "POST api/v2/website/hardware-quote",
    "description": "Smoke test for POST /api/v2/website/hardware-quote. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/hardware-quote without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1118",
    "title": "POST api/v2/website/newsletter/subscribe",
    "description": "Smoke test for POST /api/v2/website/newsletter/subscribe. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/newsletter/subscribe without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1119",
    "title": "POST api/v2/website/newsletter/unsubscribe",
    "description": "Smoke test for POST /api/v2/website/newsletter/unsubscribe. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/newsletter/unsubscribe without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1120",
    "title": "POST api/v2/website/partnership",
    "description": "Smoke test for POST /api/v2/website/partnership. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/partnership without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1121",
    "title": "POST api/v2/website/provider-registration",
    "description": "Smoke test for POST /api/v2/website/provider-registration. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/website/provider-registration without auth. Expect status < 500. Public route.  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1122",
    "title": "POST api/v2/wishlist",
    "description": "Smoke test for POST /api/v2/wishlist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/wishlist once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1123",
    "title": "GET api/v2/wishlist",
    "description": "Smoke test for GET /api/v2/wishlist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/wishlist once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1124",
    "title": "DELETE api/v2/wishlist",
    "description": "Smoke test for DELETE /api/v2/wishlist. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call DELETE /api/v2/wishlist once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1125",
    "title": "GET api/v2/zatca/compliance-summary",
    "description": "Smoke test for GET /api/v2/zatca/compliance-summary. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/zatca/compliance-summary once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1126",
    "title": "POST api/v2/zatca/enroll",
    "description": "Smoke test for POST /api/v2/zatca/enroll. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/zatca/enroll once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1127",
    "title": "GET api/v2/zatca/invoices",
    "description": "Smoke test for GET /api/v2/zatca/invoices. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/zatca/invoices once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  },
  {
    "id": "TC1128",
    "title": "GET api/v2/zatca/invoices/{invoiceId}/xml",
    "description": "Smoke test for GET /api/v2/zatca/invoices/{invoiceId}/xml. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/zatca/invoices/{invoiceId}/xml once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  PARAMETERIZED: This route has path parameters. Use the placeholder value '00000000-0000-0000-0000-000000000000' (or '1' if numeric is more natural). Acceptable outcomes: 200/201/204 (success), 404 (resource not found, route exists), 422 (validation/format error, route exists). Treat 5xx as a hard failure."
  },
  {
    "id": "TC1129",
    "title": "POST api/v2/zatca/renew",
    "description": "Smoke test for POST /api/v2/zatca/renew. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/zatca/renew once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1130",
    "title": "POST api/v2/zatca/submit-batch",
    "description": "Smoke test for POST /api/v2/zatca/submit-batch. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/zatca/submit-batch once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1131",
    "title": "POST api/v2/zatca/submit-invoice",
    "description": "Smoke test for POST /api/v2/zatca/submit-invoice. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call POST /api/v2/zatca/submit-invoice once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500).  WRITE METHOD: For POST/PUT/PATCH/DELETE, you may not have a valid body. Send an empty JSON {} body. Acceptable outcomes: 200/201/204 success, 400/404/409/422 client errors (route exists and validates). Treat 5xx as a hard failure. If the endpoint is for resource creation and you can construct a sensible payload from common fields (name, sku, code, email, phone, amount, currency='SAR', is_active=true, store_id=null), do so."
  },
  {
    "id": "TC1132",
    "title": "GET api/v2/zatca/vat-report",
    "description": "Smoke test for GET /api/v2/zatca/vat-report. AUTH: To get a Bearer token, POST /api/v2/auth/login with JSON {\"email\":\"owner@testsprite.local\",\"password\":\"Password123!\"}. The token is at response.data.token. Send Authorization: Bearer <token> on protected endpoints. Response envelope is always {success, message, data, errors?}. Call GET /api/v2/zatca/vat-report once WITHOUT auth (expect 401) and then WITH the Bearer token (expect status < 500)."
  }
]